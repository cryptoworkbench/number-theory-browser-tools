---
phase: 01-palette-unification
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Pizza Slices/pizza-slices.html
  - Factorize By Completing The Square/factorize-completing-square.html
  - index.html
autonomous: true
requirements: [PAL-01, PAL-02, PAL-03, PAL-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Congruence Wheel, the completing-the-square tool and the hub all render from the same palette as every other page (PAL-01)."
    - "None of the three pages declares a literal color of its own (PAL-02)."
    - "Toggling day/night re-themes all three pages, including the Congruence Wheel, whose theme blocks were previously inverted relative to every other tool (PAL-03)."
    - "In the completing-the-square tool, the success/answer color, the error color and the advanced-step color still mean the same things they mean in the Sieve and RSA (PAL-04)."
  artifacts:
    - Pizza Slices/pizza-slices.html
    - Factorize By Completing The Square/factorize-completing-square.html
    - index.html
  key_links:
    - "Pizza Slices declares `:root` as its DAY palette and `:root[data-theme=\"night\"]` as the override — the inverse of every other tool. Deleting both blocks removes the inversion entirely rather than normalizing it."
    - "Pizza's `var(--accent)` and `var(--accent-soft)` consumption sites need no edit: the palette supplies both names, and `--role-input` is defined as `var(--accent)`, so the selected-residue-class highlight is already role-consistent."
    - "Token-name prefix collisions (`--ink` inside `--ink-soft`, `--line` inside `--line-strong`, `--accent` inside `--accent-soft`) make a naive find-and-replace corrupting; the longer name must be rewritten first or matched exactly."
---

<objective>
Unify the three remaining pages that use only core surface, text and accent colors — the Congruence Wheel, the completing-the-square factorizer, and the `index.html` hub — onto `assets/palette.css`.

Purpose: These three are grouped because each is a small, self-contained deletion-plus-rename against the core token layer, with no diagram-role complexity of its own beyond the completing-the-square tool's three semantic accents. Grouping them keeps the wave-2 fan-out to three parallel plans while giving each page its own task and its own verification.

Output: all three pages rendering entirely from palette tokens in both themes, after which every page in the repo is unified.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-palette-unification/01-01-SUMMARY.md
@assets/palette.css
@CLAUDE.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

This plan produces no new files or symbols. It consumes tokens created by plan `01-01` (`assets/palette.css`) and leaves **zero** local custom properties in all three files — every token each one declares is a color with a palette equivalent, so every `:root` block in all three files is deleted outright.

Phase-wide rule this plan must satisfy, per file: no hex, `rgb()`, `rgba()`, `hsl()` or named color literal may remain outside CSS comments and the Google Fonts `<link>`.

## Token Mapping — Pizza Slices (Congruence Wheel)

This file is structured **inversely** to every other tool: `:root` (lines 13-23) holds the DAY values and `:root[data-theme="night"]` (lines 24-34) holds the night override, whereas every other page uses `:root` for night with a `[data-theme="day"]` override. Both blocks contain nothing but the same nine color tokens, so both are deleted in full and the inversion stops existing — there is nothing to normalize.

| Current token | Rewrite consumption sites to | Sites |
|---------------|------------------------------|-------|
| `--bg` | `var(--bg-1)` | 1 |
| `--ink` | `var(--text)` | 5 |
| `--ink-soft` | `var(--text-dim)` | 8 |
| `--line` | `var(--panel-border)` | 4 |
| `--line-strong` | `var(--panel-border-strong)` | 2 |
| `--ring-a` | `var(--surface-alt)` | 2 |
| `--card` | `var(--surface)` | 1 |
| `--accent` | **leave as `var(--accent)`** — the palette supplies this exact name | 9 |
| `--accent-soft` | **leave as `var(--accent-soft)`** — the palette supplies this exact name | 1 |

The tool's accent changes literal value from teal (`#3FD9C7` night / `#0F6E68` day) to the shared blue, which is the PAL-01 change for this page. The selected-residue-class highlight stays on `var(--accent)`, which is role-correct without an edit: the palette defines `--role-input` as `var(--accent)`, and a selected residue class is a user selection (PAL-04).

## Token Mapping — Factorize By Completing The Square

| Current token | Rewrite consumption sites to | Then |
|---------------|------------------------------|------|
| `--bg-1`, `--bg-2`, `--panel`, `--panel-border`, `--text`, `--text-dim` | unchanged names, inherited from palette.css | delete the local declarations |
| `--accent` (`#ffb454` / `#c97a1f`) | unchanged name — value becomes the shared accent | delete the local declarations |
| `--accent-2` (`#45e0c1` / `#0f9a80`) | `var(--role-result)` | delete |
| `--accent-3` (`#c792ea` / `#8a4fc4`) | `var(--role-special)` | delete |
| `--danger` | `var(--role-warn)` | delete |
| `--good` | `var(--role-result)` | delete |
| `--table-head-bg` (`#171226` / `#f1eafc`) | `var(--surface-alt)` | delete |

`--good` and `--accent-2` both map to `--role-result`: they are already the same literal value in both themes today (`#45e0c1`/`#0f9a80` versus `#0f9a80`), so nothing is lost. One hardcoded literal to replace: line 156 `color: #1a1327` becomes `var(--accent-ink)`.

## Token Mapping — index.html (hub)

`:root` (lines 13-22) and `:root[data-theme="day"]` (lines 23-32) declare the eight tokens that plan `01-01` lifted verbatim into `assets/palette.css`, under identical names and — with one exception — identical values. Delete both blocks; **no consumption site needs any edit**, because every name matches the palette. The one value change is day `--accent-2`, which moves from `#0f6e68` to `#0f9a80` so that the secondary accent and the `--role-result` role are one value; confirm the hub still reads correctly in day mode.

<tasks>

<task type="auto">
  <name>Task 1: Unify the Congruence Wheel and remove its inverted theme blocks</name>
  <files>Pizza Slices/pizza-slices.html</files>
  <read_first>
    - assets/palette.css (available token names and values)
    - Pizza Slices/pizza-slices.html lines 1-13 (head; confirm plan 01-01 already added the palette.css link before site.css)
    - Pizza Slices/pizza-slices.html lines 13-34 (the two inverted `:root` blocks)
    - Pizza Slices/pizza-slices.html lines 34-250 (every `var()` consumption site — the wheel sectors, ring bands, labels, cards and controls)
  </read_first>
  <precondition>`assets/palette.css` exists, declares `--bg-1`, `--text`, `--text-dim`, `--panel-border`, `--panel-border-strong`, `--surface`, `--surface-alt`, `--accent` and `--accent-soft`, and is already linked from this page's `<head>` before `../assets/site.css` (plan 01-01 outputs). Halt if the link is missing — every `var()` in this task would resolve to nothing.</precondition>
  <action>
Apply the "Token Mapping — Pizza Slices" table, satisfying PAL-01, PAL-02 and PAL-03. Delete both `:root` blocks in full, then rewrite the seven renamed consumption sites and leave the `var(--accent)` and `var(--accent-soft)` sites untouched.

Rewrite carefully: three of these token names are prefixes of others in the same file. Rewrite `var(--ink-soft)` before `var(--ink)`, and `var(--line-strong)` before `var(--line)`, or match each name exactly including its closing parenthesis — a naive substring replace turns `var(--ink-soft)` into `var(--text-soft)` and `var(--line-strong)` into `var(--panel-border-strong)` where `var(--panel-border-strong)` was not intended, both of which resolve to nothing and render as an unstyled page. Do not rewrite `var(--accent-soft)` while rewriting `var(--accent)` — neither needs any change.

Note for the summary: this page was the only one in the repo whose `:root` held the day palette with a `[data-theme="night"]` override, the inverse of every other tool. Deleting both blocks removes the inconsistency rather than normalizing it, so no follow-up is needed.

Do not touch the `<script>` block, the polar-geometry or `annularSectorPath` math, any layout rule, or the `@media` queries.
  </action>
  <verify>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && F="Pizza Slices/pizza-slices.html"; grep -cE '^[[:space:]]*--[a-z0-9-]+[[:space:]]*:' "$F" | grep -qx 0 || echo LOCAL-TOKENS-REMAIN; grep -c ':root' "$F" | grep -qx 0 || echo ROOT-BLOCK-REMAINS; for t in ink ink-soft line line-strong ring-a card bg; do grep -q "var(--$t)" "$F" && echo "OLD-TOKEN-REF $t"; done; for t in bg-1 text text-dim panel-border panel-border-strong surface surface-alt accent; do grep -q "var(--$t)" "$F" || echo "NEW-TOKEN-UNUSED $t"; done; echo WHEEL-CHECKED</automated>
    <fails_when>the command prints `LOCAL-TOKENS-REMAIN`, `ROOT-BLOCK-REMAINS`, any `OLD-TOKEN-REF` (a stale reference that now resolves to nothing) or any `NEW-TOKEN-UNUSED` line, or omits `WHEEL-CHECKED`</fails_when>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && F="Pizza Slices/pizza-slices.html"; grep -nEi '#[0-9a-f]{3}\b|#[0-9a-f]{6}\b|rgba?\([0-9]|hsla?\(' "$F" | grep -vE ':[[:space:]]*(/\*|\*)' | grep -v 'fonts.googleapis\|fonts.gstatic' | sed 's/^/FILE-LITERAL /'; echo FILE-SWEPT</automated>
    <fails_when>the command prints any `FILE-LITERAL` line</fails_when>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && sed -n '/<\/style>/,$p' "Pizza Slices/pizza-slices.html" | md5sum | cut -d' ' -f1 | grep -qx 8c1ff16d977dec920ff6289688da6d51 && echo SCRIPT-UNTOUCHED || echo SCRIPT-CHANGED</automated>
    <fails_when>the command prints `SCRIPT-CHANGED` — the region from `</style>` to end of file must still hash to `8c1ff16d977dec920ff6289688da6d51`, its pre-phase value, proving the polar-geometry and sector-path math were not touched</fails_when>
    <human-check>Open the page, change N, select a residue class, and toggle day/night. Confirm the wheel sectors, the alternating ring bands, the wedge labels, the selected-class highlight and the surrounding card are all correctly themed in both modes — and in particular that day mode is still light and night mode still dark, since this page's theme blocks were previously inverted.</human-check>
  </verify>
  <acceptance_criteria>
    - `Pizza Slices/pizza-slices.html` declares zero custom properties and contains zero `:root` selectors.
    - No `var(--ink)`, `var(--ink-soft)`, `var(--line)`, `var(--line-strong)`, `var(--ring-a)`, `var(--card)` or `var(--bg)` reference remains.
    - `var(--bg-1)`, `var(--text)`, `var(--text-dim)`, `var(--panel-border)`, `var(--panel-border-strong)`, `var(--surface)`, `var(--surface-alt)` and `var(--accent)` all appear.
    - The file contains no color literal outside comments and the Google Fonts `<link>`.
    - The `<script>` block is byte-identical to its pre-phase state: `sed -n '/<\/style>/,$p' "Pizza Slices/pizza-slices.html" | md5sum` prints `8c1ff16d977dec920ff6289688da6d51`.
  </acceptance_criteria>
  <done>The Congruence Wheel renders from palette tokens with correct day and night modes, and its inverted theme-block structure no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Unify the completing-the-square factorizer onto core and role tokens</name>
  <files>Factorize By Completing The Square/factorize-completing-square.html</files>
  <read_first>
    - assets/palette.css (available token names and values)
    - Factorize By Completing The Square/factorize-completing-square.html lines 1-11 (head; confirm the palette.css link precedes site.css)
    - Factorize By Completing The Square/factorize-completing-square.html lines 11-38 (both `:root` blocks)
    - Factorize By Completing The Square/factorize-completing-square.html lines 38-261 (every `var()` consumption site, the trials table head, and line 156's hardcoded ink)
  </read_first>
  <precondition>`assets/palette.css` exists, declares `--accent`, `--accent-ink`, `--surface-alt`, `--role-result`, `--role-special` and `--role-warn`, and is already linked from this page's `<head>` before `../assets/site.css` (plan 01-01 outputs). Halt if the link is missing.</precondition>
  <action>
Apply the "Token Mapping — Factorize By Completing The Square" table, satisfying PAL-01, PAL-02 and PAL-04. Delete both `:root` blocks in full: the six core tokens and `--accent` are inherited from the palette under the same names, and the remaining five are rewritten at their consumption sites to `var(--role-result)`, `var(--role-special)`, `var(--role-warn)` and `var(--surface-alt)`.

The role mapping is what makes this page's colors mean the same thing as the Sieve's and RSA's: a successful trial / the found factorization takes `--role-result`, an invalid or no-solution state takes `--role-warn`, and the advanced or optional step takes `--role-special`. Replace line 156's hardcoded `color: #1a1327` with `var(--accent-ink)` so the label ink flips with the theme.

Watch the prefix collision here too: rewrite `var(--accent-3)` and `var(--accent-2)` before touching any `var(--accent)` site, and leave plain `var(--accent)` references alone.

Do not touch the `<script>` block, the Fermat-style trial loop, the playback controls, or any layout rule.
  </action>
  <verify>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && F="Factorize By Completing The Square/factorize-completing-square.html"; grep -cE '^[[:space:]]*--[a-z0-9-]+[[:space:]]*:' "$F" | grep -qx 0 || echo LOCAL-TOKENS-REMAIN; grep -c ':root' "$F" | grep -qx 0 || echo ROOT-BLOCK-REMAINS; for t in accent-2 accent-3 danger good table-head-bg; do grep -q "var(--$t)" "$F" && echo "OLD-TOKEN-REF $t"; done; for t in role-result role-special role-warn surface-alt accent-ink; do grep -q "var(--$t)" "$F" || echo "NEW-TOKEN-UNUSED $t"; done; echo SQUARE-CHECKED</automated>
    <fails_when>the command prints `LOCAL-TOKENS-REMAIN`, `ROOT-BLOCK-REMAINS`, any `OLD-TOKEN-REF` or any `NEW-TOKEN-UNUSED` line, or omits `SQUARE-CHECKED`</fails_when>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && F="Factorize By Completing The Square/factorize-completing-square.html"; grep -nEi '#[0-9a-f]{3}\b|#[0-9a-f]{6}\b|rgba?\([0-9]|hsla?\(' "$F" | grep -vE ':[[:space:]]*(/\*|\*)' | grep -v 'fonts.googleapis\|fonts.gstatic' | sed 's/^/FILE-LITERAL /'; echo FILE-SWEPT</automated>
    <fails_when>the command prints any `FILE-LITERAL` line</fails_when>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && sed -n '/<\/style>/,$p' "Factorize By Completing The Square/factorize-completing-square.html" | md5sum | cut -d' ' -f1 | grep -qx a86d143628956ed109ffdeb4f5c4e8b2 && echo SCRIPT-UNTOUCHED || echo SCRIPT-CHANGED</automated>
    <fails_when>the command prints `SCRIPT-CHANGED` — the region from `</style>` to end of file must still hash to `a86d143628956ed109ffdeb4f5c4e8b2`, its pre-phase value, proving the trial loop and playback controls were not touched</fails_when>
    <human-check>Open the page, run a factorization that succeeds and one that does not, stepping through the trials, and toggle day/night. Confirm the trials-table header, the successful-trial highlight, the failure/error state and any advanced-step callout are each legible and distinguishable in both themes.</human-check>
  </verify>
  <acceptance_criteria>
    - The file declares zero custom properties and contains zero `:root` selectors.
    - No `var(--accent-2)`, `var(--accent-3)`, `var(--danger)`, `var(--good)` or `var(--table-head-bg)` reference remains.
    - `var(--role-result)`, `var(--role-special)`, `var(--role-warn)`, `var(--surface-alt)` and `var(--accent-ink)` all appear.
    - The file contains no color literal outside comments and the Google Fonts `<link>`.
    - The `<script>` block is byte-identical to its pre-phase state: `sed -n '/<\/style>/,$p' "Factorize By Completing The Square/factorize-completing-square.html" | md5sum` prints `a86d143628956ed109ffdeb4f5c4e8b2`.
  </acceptance_criteria>
  <done>The completing-the-square tool renders from core and role tokens in both themes, with success, failure and advanced-step colors matching their meanings elsewhere on the site.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the hub's duplicated palette declarations</name>
  <files>index.html</files>
  <read_first>
    - assets/palette.css (confirm the eight token names and values were lifted from this page by plan 01-01)
    - index.html lines 1-12 (head; confirm the palette.css link precedes site.css)
    - index.html lines 12-35 (the two `:root` blocks being deleted)
    - index.html lines 35-131 (confirm every consumption site uses one of the eight names and so needs no edit)
  </read_first>
  <precondition>`assets/palette.css` exists, declares `--bg-1`, `--bg-2`, `--panel`, `--panel-border`, `--text`, `--text-dim`, `--accent` and `--accent-2`, and is already linked from `index.html` before `assets/site.css` (plan 01-01 outputs). Halt if the link is missing — deleting the local block without the link would leave the hub unstyled.</precondition>
  <action>
Delete both `:root` blocks from `index.html` so the hub consumes `assets/palette.css` instead of redeclaring it (PAL-02). Every one of the eight token names matches the palette, so no consumption site changes — verify that by grep before and after rather than by editing rules.

One value changes as a result: in day mode `--accent-2` becomes `#0f9a80` instead of `#0f6e68`, because plan 01-01 aligned the secondary accent with the `--role-result` role. Confirm in the browser that whatever the hub uses `--accent-2` for still reads correctly in day mode (PAL-03).

Do not change the hero copy, the tool card grid, the card links, the header markup or any layout rule. This task should be a pure deletion.
  </action>
  <verify>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && F="index.html"; grep -cE '^[[:space:]]*--[a-z0-9-]+[[:space:]]*:' "$F" | grep -qx 0 || echo LOCAL-TOKENS-REMAIN; grep -c ':root' "$F" | grep -qx 0 || echo ROOT-BLOCK-REMAINS; grep -nEi '#[0-9a-f]{3}\b|#[0-9a-f]{6}\b|rgba?\([0-9]|hsla?\(' "$F" | grep -vE ':[[:space:]]*(/\*|\*)' | grep -v 'fonts.googleapis\|fonts.gstatic' | sed 's/^/FILE-LITERAL /'; grep -q 'assets/palette.css' "$F" || echo NO-PALETTE-LINK; echo HUB-CHECKED</automated>
    <fails_when>the command prints `LOCAL-TOKENS-REMAIN`, `ROOT-BLOCK-REMAINS`, `NO-PALETTE-LINK` or any `FILE-LITERAL` line, or omits `HUB-CHECKED`</fails_when>
    <automated>cd /home/mainaccount/Claude/number-theory-browser-tools && sed -n '/<\/style>/,$p' index.html | md5sum | cut -d' ' -f1 | grep -qx 17cf6038aefd4d21e61d00c81a517255 && echo MARKUP-UNTOUCHED || echo MARKUP-CHANGED; n=$(grep -c 'assets/palette.css' index.html); [ "$n" = 1 ] || echo "LINK-COUNT=$n"</automated>
    <fails_when>the command prints `MARKUP-CHANGED` or a `LINK-COUNT=` line — everything from `</style>` to end of file must still hash to `17cf6038aefd4d21e61d00c81a517255`, its pre-phase value, proving this task deleted a style block and did not touch the hero copy, the tool card grid or the header markup</fails_when>
    <human-check>Open `index.html` and toggle day/night. Confirm the hero text, the tool cards, their hover state and the footer all render correctly in both themes, and that the page is visually identical to before apart from the day-mode secondary accent.</human-check>
  </verify>
  <acceptance_criteria>
    - `index.html` declares zero custom properties and contains zero `:root` selectors.
    - `index.html` contains no color literal outside comments and the Google Fonts `<link>`.
    - The palette.css `<link>` is present.
    - Everything from `</style>` to end of file still hashes to `17cf6038aefd4d21e61d00c81a517255`, so the change was a style-block deletion and nothing else.
  </acceptance_criteria>
  <done>The hub consumes the shared palette rather than declaring its own copy, and renders unchanged in both themes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none in scope)* | Three static HTML pages. This plan deletes CSS custom property declarations and renames `var()` references; no `<script>` block is touched, no input is parsed, no network request beyond the pre-existing Google Fonts `<link>`, no storage, no auth, no server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `Pizza Slices/pizza-slices.html`, `Factorize By Completing The Square/factorize-completing-square.html`, `index.html` (static asset styling only) | low | accept | No applicable threat — styling only, with no new dependency, network call or code execution path. |
| T-01-04-b | Denial of Service | unresolvable `var()` after a botched prefix-collision rewrite | low | mitigate | Not a security threat but a real availability failure for the page (an unstyled, effectively unreadable tool). Mitigated by the explicit rewrite-longest-name-first instruction in Task 1, the per-task `OLD-TOKEN-REF` / `NEW-TOKEN-UNUSED` grep gates, and the `precondition` that halts if the palette link is missing. |

No package-manager installs occur in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
1. All three files declare zero custom properties and contain zero `:root` selectors (automated, Tasks 1-3).
2. No stale `var()` reference to a deleted token remains, and every expected replacement token is actually in use (automated, Tasks 1-2).
3. Per-file color-literal sweeps are clean (automated, Tasks 1-3).
4. The hub's change is deletions only (automated, Task 3).
5. Browser checks in both themes: wheel sectors and ring bands, trials table and verdict states, hub cards and hover (human, Tasks 1-3).
</verification>

<success_criteria>
- The Congruence Wheel, the completing-the-square tool and the hub each declare no colors and render entirely from `assets/palette.css`.
- Day/night toggling is correct on all three, including the previously inverted Congruence Wheel.
- Success, failure and advanced-step colors in the completing-the-square tool match the meanings the Sieve and RSA use.
- No page's layout, math or playback behavior changed.
</success_criteria>

<output>
Create `.planning/phases/01-palette-unification/01-04-SUMMARY.md` when done. Note explicitly that the Congruence Wheel's inverted theme-block structure was removed, so plan 01-05's sweep does not need to look for it again.
</output>
