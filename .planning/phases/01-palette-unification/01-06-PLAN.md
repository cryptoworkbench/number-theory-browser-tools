---
phase: 01-palette-unification
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - Sieve Of Eratosthenes/sieve-of-eratosthenes.html
  - Factorize By Completing The Square/factorize-completing-square.html
  - RSA Examplifier/rsa-examplifier.html
  - Pizza Slices/pizza-slices.html
  - index.html
  - Christmas Trees/factor-tree.html
  - assets/theme.js
autonomous: true
requirements: [PAL-01, PAL-02, PAL-03, PAL-04]
gap_closure: true
gap_ids: [G-01-1a, G-01-1b]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The sticky nav header (.site-header) renders full-width and edge-pinned identically on all six pages — index.html, Christmas Trees, Sieve of Eratosthenes, Factorize by Completing the Square, Pizza Slices, and RSA Examplifier — zero left offset, zero side insets (closes G-01-1a)."
    - "Each of the four previously-affected pages keeps its exact original page-gutter spacing around its own content — the padding moved from <body> onto that page's content container, it was not deleted or changed in value."
    - "Toggling day/night on one page and navigating (via the shared nav header's own links) to a different tool preserves the selected theme on the destination page, including in an environment where localStorage throws on every access — Safari's file:// SecurityError restriction (closes G-01-1b)."
    - "Normal localStorage-based persistence and the existing cross-tab live-sync (the storage event listener in assets/theme.js) continue to work unchanged wherever localStorage is available (Chrome, Firefox)."
  artifacts:
    - Sieve Of Eratosthenes/sieve-of-eratosthenes.html
    - Factorize By Completing The Square/factorize-completing-square.html
    - RSA Examplifier/rsa-examplifier.html
    - Pizza Slices/pizza-slices.html
    - assets/theme.js
    - index.html
    - Christmas Trees/factor-tree.html
  key_links:
    - "<body> (unpadded on all six pages after this plan) -> .site-header (position:sticky, full-bleed, assets/site.css) via the CSS containing-block relationship — this is the exact mechanism the debug session measured (header.left/top === body.padding-left/top)."
    - "assets/theme.js setTheme() -> window.name (same-tab, cross-origin-safe) -> next page's pre-paint <head> script -> data-theme attribute, set before first paint."
---

<objective>
Close the two major-severity gaps from Phase 01's human UAT sign-off (01-UAT.md, test 1): the shared nav header floats/insets on 4 of 6 pages instead of rendering edge-to-edge everywhere (G-01-1a), and the day/night theme selection does not survive navigation to another tool in any environment where `localStorage` access throws — Safari's file:// restriction (G-01-1b). Both root causes were already fully diagnosed by prior debug sessions (`.planning/debug/nav-header-not-full-width.md`, `.planning/debug/theme-not-persistent-across-pages.md`); this plan implements the fixes.

Both defects are pre-existing site-chrome bugs (the header bug predates Phase 01 entirely; the theme bug is a platform limitation combined with a silent-catch convention that also predates Phase 01's specific changes). They were simply the first time a human UAT pass compared all six pages side by side. Fixing them completes the omnibus "developer sign-off on the unified palette" UAT test that covers PAL-01 through PAL-04.

**Fix-direction correction (read before implementing Task 2):** the theme debug session's suggested fallback direction was "a document.cookie-based (or equivalent) fallback." That mechanism was tried and empirically rejected during this plan's own research: `document.cookie` writes are silently dropped (no exception, nothing persisted) on `file://` origins in both Chromium and WebKit, because cookies require a host/domain component that `file://` URLs lack. The mechanism this plan actually implements is `window.name`, which survives same-tab navigation across different `file://` documents regardless of any storage-permission model — verified empirically against this exact repo before writing this plan (a throwing-localStorage simulation failed against the current code and passed after applying the `window.name` fix, across two consecutive same-tab hops via the real nav-header links).

Purpose: Close the last two open items blocking Phase 01's human sign-off, without touching the palette token work itself (assets/palette.css, role-color mappings) — this plan touches only page-gutter CSS placement and theme-persistence plumbing.
Output: Six pages with an edge-pinned nav header in every page and a theme selection that survives cross-tool navigation even when localStorage is unavailable.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/phases/01-palette-unification/01-UAT.md
@.planning/debug/nav-header-not-full-width.md
@.planning/debug/theme-not-persistent-across-pages.md
@assets/site.css
@assets/theme.js
</context>

## Gap-to-task map

| Gap | Root cause (already diagnosed) | Task |
|-----|---------------------------------|------|
| G-01-1a | `.site-header` (assets/site.css) is a full-bleed `position:sticky` block with no self-protection against a padded ancestor; 4 of 6 pages put a page-gutter `padding` directly on `<body>`, which is `.site-header`'s containing block, shrinking/offsetting it by exactly that padding. `index.html` and `Christmas Trees/factor-tree.html` are correct only because they happen to declare zero body padding. | Task 1 |
| G-01-1b | AND-gate: (1) Safari/WebKit throws `SecurityError` on ANY `localStorage` access from a page loaded via `file://`; (2) every localStorage access point in this repo (assets/theme.js and every page's pre-paint `<head>` script) silently swallows that error and falls back to `'night'` with no alternate persistence path. The toggle still visually flips the *current* page (the DOM attribute is set unconditionally), so it looks like it worked, but nothing is saved — the next page load reverts to night. | Task 2 |

<tasks>

<task type="auto">
  <name>Task 1: Un-inset the shared nav header — move page-gutter padding off &lt;body&gt; onto each page's own content container (G-01-1a)</name>
  <files>Sieve Of Eratosthenes/sieve-of-eratosthenes.html, Factorize By Completing The Square/factorize-completing-square.html, RSA Examplifier/rsa-examplifier.html, Pizza Slices/pizza-slices.html</files>
  <action>
    This is the debug session's "Preferred" fix direction (fix condition B of the AND-gate: move the gutter off `<body>` onto the content wrapper that already exists as the header's next sibling). Do NOT touch `assets/site.css`, `index.html`, or `Christmas Trees/factor-tree.html` — the shared header rule is correctly written for full-bleed behavior already, and the latter two pages already declare zero body padding (they are the two currently-correct pages).

    On each of the 4 files below, remove exactly one `padding` declaration from the page's own `body{...}` rule (leave every other declaration in that rule — the background gradients, `background-attachment`, `min-height`, `transition` — untouched), and add that exact same literal value as the first declaration inside the page's existing content-container rule, immediately after its opening brace. Do not change the literal value itself; only relocate it.

    - `Sieve Of Eratosthenes/sieve-of-eratosthenes.html`: remove the line `padding: clamp(12px, 3vw, 32px);` from the `body{...}` rule (the one with the `radial-gradient`/`linear-gradient` background, not the earlier `html,body{...}` rule). Add `padding: clamp(12px, 3vw, 32px);` as the first line inside `.app{`, before its `max-width: 1080px;` line.
    - `Factorize By Completing The Square/factorize-completing-square.html`: same pattern — remove `padding: clamp(12px, 3vw, 32px);` from its gradient `body{...}` rule, add it as the first line inside `.app{`, before its `max-width: 980px;` line.
    - `RSA Examplifier/rsa-examplifier.html`: remove `padding: clamp(12px, 3vw, 32px);` from its gradient `body{...}` rule. Its `.app` rule is a single-line rule (`.app{ max-width: 1040px; margin:0 auto; display:flex; flex-direction:column; gap:18px; }`) — insert `padding: clamp(12px, 3vw, 32px); ` immediately after `.app{ ` so it becomes the first declaration on that same line.
    - `Pizza Slices/pizza-slices.html`: remove the line `padding:44px 22px 64px;` from its `body{...}` rule (note this page's asymmetric value differs from the other three — preserve it exactly, do not normalize it to `clamp(12px, 3vw, 32px)`). Its container rule is `.wrap{ max-width:1120px; margin:0 auto; }` on a single line — change it to `.wrap{ max-width:1120px; margin:0 auto; padding:44px 22px 64px; }`.

    All four content containers (`*{box-sizing:border-box;}` is already declared globally in every one of these files) render their padding *inside* their existing `max-width`, so this cannot create a double-gutter: nothing else in any of these four files applies a second padding to the same box. After this change, `<body>` carries zero padding on all six pages, so `.site-header`'s containing block is unpadded everywhere and it renders full-bleed identically on every page.
  </action>
  <verify>
    <automated>
Run from the repository root (resolves whichever Chrome/Chromium binary is on PATH; zero npm dependencies — uses Node's built-in `fetch`/`WebSocket`, requires Node >= 22):

CHROME_BIN=$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser) && CHROME_BIN="$CHROME_BIN" node <(cat <<'NODE_EOF'
// Zero-dependency CDP check (Node >=22 built-in fetch + WebSocket, no npm deps).
// Proves G-01-1a is closed: .site-header spans the full viewport width with
// zero left offset on all six pages (root cause: <body> padding shrank/offset
// it because .site-header has no self-protection against a padded ancestor).
// Also guards against a double-gutter regression: each page's content
// container (.app or .wrap) must carry exactly the ORIGINAL padding value
// that used to live on <body> -- not zero (dropped) and not doubled.
const { spawn } = require('child_process');
const path = require('path');
function sleep(ms){ return new Promise(r => setTimeout(r, ms)); }

const PAGES = [
  { file: 'index.html', container: null, expectPaddingLeft: null },
  { file: 'Christmas Trees/factor-tree.html', container: null, expectPaddingLeft: null },
  { file: 'Sieve Of Eratosthenes/sieve-of-eratosthenes.html', container: '.app', expectPaddingLeft: 32 },
  { file: 'Factorize By Completing The Square/factorize-completing-square.html', container: '.app', expectPaddingLeft: 32 },
  { file: 'Pizza Slices/pizza-slices.html', container: '.wrap', expectPaddingLeft: 22 },
  { file: 'RSA Examplifier/rsa-examplifier.html', container: '.app', expectPaddingLeft: 32 },
];

async function main(){
  const port = 9231;
  const chrome = spawn(process.env.CHROME_BIN || 'google-chrome', ['--headless=new','--disable-gpu','--no-sandbox',`--remote-debugging-port=${port}`,'--window-size=1280,900'], { stdio: 'ignore' });
  await sleep(1200);
  let failures = 0;
  try {
    const version = await (await fetch(`http://127.0.0.1:${port}/json/version`)).json();
    const ws = new WebSocket(version.webSocketDebuggerUrl);
    await new Promise((res, rej) => { ws.addEventListener('open', res); ws.addEventListener('error', rej); });
    let id = 1;
    function browserRpc(method, params){
      return new Promise((resolve, reject) => {
        const mid = id++;
        function onMsg(ev){ const msg = JSON.parse(ev.data); if (msg.id === mid && !msg.sessionId) { ws.removeEventListener('message', onMsg); msg.error?reject(new Error(JSON.stringify(msg.error))):resolve(msg.result); } }
        ws.addEventListener('message', onMsg);
        ws.send(JSON.stringify({ id: mid, method, params }));
      });
    }
    const target = await browserRpc('Target.createTarget', { url: 'about:blank' });
    const attach = await browserRpc('Target.attachToTarget', { targetId: target.targetId, flatten: true });
    const sessionId = attach.sessionId;
    function rpc(method, params){
      return new Promise((resolve, reject) => {
        const mid = id++;
        function onMsg(ev){ const msg = JSON.parse(ev.data); if (msg.id === mid && msg.sessionId === sessionId) { ws.removeEventListener('message', onMsg); msg.error?reject(new Error(JSON.stringify(msg.error))):resolve(msg.result); } }
        ws.addEventListener('message', onMsg);
        ws.send(JSON.stringify({ id: mid, method, params, sessionId }));
      });
    }
    await rpc('Page.enable', {});
    await rpc('Runtime.enable', {});
    async function navigate(url){ await rpc('Page.navigate', { url: 'file://' + path.resolve(url) }); await sleep(500); }
    async function evalOn(expr){ const r = await rpc('Runtime.evaluate', { expression: expr, returnByValue: true, awaitPromise: true }); if (r.exceptionDetails) throw new Error(JSON.stringify(r.exceptionDetails)); return r.result.value; }

    for (const pg of PAGES) {
      await navigate(pg.file);
      const data = JSON.parse(await evalOn(`(function(){
        var h = document.querySelector('.site-header');
        var r = h.getBoundingClientRect();
        var pad = null;
        ${pg.container ? `var c = document.querySelector('${pg.container}'); pad = parseFloat(getComputedStyle(c).paddingLeft);` : ''}
        return JSON.stringify({left:r.left, width:r.width, vw:document.documentElement.clientWidth, pad:pad});
      })()`));
      let ok = data.left === 0 && data.width >= data.vw - 1;
      let note = `left=${data.left} width=${data.width} vw=${data.vw}`;
      if (pg.expectPaddingLeft !== null) {
        const padOk = Math.abs(data.pad - pg.expectPaddingLeft) < 1;
        ok = ok && padOk;
        note += ` ${pg.container}.paddingLeft=${data.pad} (expect ${pg.expectPaddingLeft})`;
      }
      console.log(`${ok ? 'OK  ' : 'FAIL'} ${pg.file}: ${note}`);
      if (!ok) failures++;
    }
    ws.close();
  } finally {
    chrome.kill();
  }
  if (failures > 0) { console.error(`${failures} page(s) failed the full-width header / gutter check`); process.exit(1); }
  console.log('PASS: header is full-width and edge-pinned on all six pages; no double-gutter on any content container');
}
main().catch(e=>{ console.error('ERR', e); process.exit(1); });
NODE_EOF
)

Expected output: 6 `OK` lines followed by `PASS: header is full-width and edge-pinned on all six pages; no double-gutter on any content container`, exit code 0. This exact script was validated against the current (unfixed) repo before this plan was written — it correctly reports 4 `FAIL` lines (Sieve, Completing-the-Square, Pizza Slices, RSA) and 2 `OK` lines (index.html, Christmas Trees) prior to this task's edit.
    </automated>
    <human-check>Open each of the six pages directly in a real browser (not headless) at a normal desktop width and confirm the header visually touches all three edges (left, right, top) identically on every page, with no side margins or top gap on any of them.</human-check>
  </verify>
  <done>All six pages' `.site-header` spans the full viewport width with zero left offset; the four previously-affected pages' own content (`.app`/`.wrap`) retains its exact original gutter spacing, just relocated from `<body>`.</done>
</task>

<task type="auto">
  <name>Task 2: Add a same-tab persistence fallback for theme selection when localStorage throws (G-01-1b)</name>
  <files>assets/theme.js, index.html, Christmas Trees/factor-tree.html, Sieve Of Eratosthenes/sieve-of-eratosthenes.html, Factorize By Completing The Square/factorize-completing-square.html, Pizza Slices/pizza-slices.html, RSA Examplifier/rsa-examplifier.html</files>
  <action>
    In `assets/theme.js`, layer a `window.name` fallback onto the existing `localStorage`-primary persistence, without removing any existing try/catch:
    - `setTheme(theme)` must, in addition to its existing `try{ localStorage.setItem(STORAGE_KEY, theme); }catch(e){}`, unconditionally also do `try{ window.name = theme; }catch(e){}` every time the theme changes — before calling `applyTheme`.
    - `readTheme()` must first attempt `localStorage.getItem(STORAGE_KEY)` inside its existing try/catch; if that throws OR the result is not exactly `'day'` or `'night'`, fall back to `window.name`, using it ONLY if it is exactly `'day'` or `'night'` (strict equality both ways — never write an arbitrary `window.name` value into `data-theme`). Return `'night'` if neither source resolves to a valid value, preserving the existing default behavior.
    - Add a short comment above the IIFE explaining why `window.name` was chosen over `document.cookie`: cookies require a host/domain component that `file://` URLs lack, so `document.cookie` writes are silently dropped (no exception, nothing persisted) on `file://` in both Chromium and WebKit — verified empirically for this repo, not assumed from spec. `window.name` survives same-tab navigation across different `file://` documents regardless of any storage-permission model, matching this project's zero-server, open-the-file-directly deployment model (no build system, no server — per CLAUDE.md).
    - Leave `applyTheme()` and the `storage` event cross-tab-sync listener completely unchanged.

    On EACH of the six pages' `<head>`, replace the pre-paint inline script (byte-identical across all six today) with a version that mirrors the same localStorage-then-window.name fallback logic self-contained inline — this script runs synchronously before `assets/theme.js` loads (it is not deferred), so it cannot call into that file and must repeat the same two-step read itself.

    Exact current line (present verbatim in all six files, on line 5 of `<head>`): `<script>(function(){try{var t=localStorage.getItem('site-theme')==='day'?'day':'night';document.documentElement.setAttribute('data-theme',t);}catch(e){document.documentElement.setAttribute('data-theme','night');}})();</script>`

    Replace it with: `<script>(function(){var t=null;try{t=localStorage.getItem('site-theme');}catch(e){t=null;}if(t!=='day'&&t!=='night'){t=(window.name==='day'||window.name==='night')?window.name:null;}document.documentElement.setAttribute('data-theme',t==='day'?'day':'night');})();</script>`

    Apply this exact replacement to all six files: `index.html`, `Christmas Trees/factor-tree.html`, `Sieve Of Eratosthenes/sieve-of-eratosthenes.html`, `Factorize By Completing The Square/factorize-completing-square.html`, `Pizza Slices/pizza-slices.html`, `RSA Examplifier/rsa-examplifier.html`. Change nothing else on the line (viewport meta, title, and every other `<head>` line stay exactly as-is).
  </action>
  <verify>
    <automated>
Run from the repository root (same zero-dependency Node/CDP approach as Task 1's verify; Node >= 22 required):

CHROME_BIN=$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser) && CHROME_BIN="$CHROME_BIN" node <(cat <<'NODE_EOF'
// Zero-dependency CDP check (Node >=22 built-in fetch + WebSocket, no npm deps).
// Proves G-01-1b is closed: simulates Safari/WebKit's file:// SecurityError by
// making window.localStorage throw on ANY access, toggles the real theme
// switch on one page, then follows the real nav-header links (same-tab
// navigation, exactly how a user moves between tools) across two hops and
// confirms the chosen theme survives via the window.name fallback.
const { spawn } = require('child_process');
const path = require('path');
function sleep(ms){ return new Promise(r => setTimeout(r, ms)); }

async function main(){
  const port = 9232;
  const chrome = spawn(process.env.CHROME_BIN || 'google-chrome', ['--headless=new','--disable-gpu','--no-sandbox',`--remote-debugging-port=${port}`,'--window-size=1280,900'], { stdio: 'ignore' });
  await sleep(1200);
  try {
    const version = await (await fetch(`http://127.0.0.1:${port}/json/version`)).json();
    const ws = new WebSocket(version.webSocketDebuggerUrl);
    await new Promise((res, rej) => { ws.addEventListener('open', res); ws.addEventListener('error', rej); });
    let id = 1;
    function browserRpc(method, params){
      return new Promise((resolve, reject) => {
        const mid = id++;
        function onMsg(ev){ const msg = JSON.parse(ev.data); if (msg.id === mid && !msg.sessionId) { ws.removeEventListener('message', onMsg); msg.error?reject(new Error(JSON.stringify(msg.error))):resolve(msg.result); } }
        ws.addEventListener('message', onMsg);
        ws.send(JSON.stringify({ id: mid, method, params }));
      });
    }
    const target = await browserRpc('Target.createTarget', { url: 'about:blank' });
    const attach = await browserRpc('Target.attachToTarget', { targetId: target.targetId, flatten: true });
    const sessionId = attach.sessionId;
    function rpc(method, params){
      return new Promise((resolve, reject) => {
        const mid = id++;
        function onMsg(ev){ const msg = JSON.parse(ev.data); if (msg.id === mid && msg.sessionId === sessionId) { ws.removeEventListener('message', onMsg); msg.error?reject(new Error(JSON.stringify(msg.error))):resolve(msg.result); } }
        ws.addEventListener('message', onMsg);
        ws.send(JSON.stringify({ id: mid, method, params, sessionId }));
      });
    }
    await rpc('Page.enable', {});
    await rpc('Runtime.enable', {});
    // Installed once; CDP re-fires it before every subsequent document in this
    // target, matching Safari's per-page-load SecurityError on file://.
    await rpc('Page.addScriptToEvaluateOnNewDocument', {
      source: `Object.defineProperty(window, 'localStorage', { get(){ throw new DOMException('The operation is insecure.', 'SecurityError'); } });`
    });
    async function navigateDirect(url){ await rpc('Page.navigate', { url: 'file://' + path.resolve(url) }); await sleep(700); }
    async function evalOn(expr){ const r = await rpc('Runtime.evaluate', { expression: expr, returnByValue: true, awaitPromise: true }); if (r.exceptionDetails) throw new Error('Eval error: ' + JSON.stringify(r.exceptionDetails)); return r.result.value; }

    await navigateDirect('Sieve Of Eratosthenes/sieve-of-eratosthenes.html');
    const initialTheme = await evalOn(`document.documentElement.getAttribute('data-theme')`);
    await evalOn(`document.getElementById('theme-switch-input').click()`);
    await sleep(200);
    const afterToggle = await evalOn(`document.documentElement.getAttribute('data-theme')`);

    await evalOn(`document.querySelector('.site-nav a[href*="RSA"]').click()`);
    await sleep(800);
    const secondPageTheme = await evalOn(`document.documentElement.getAttribute('data-theme')`);
    const secondPageUrl = await evalOn(`location.href`);

    await evalOn(`document.querySelector('.site-nav a[href*="Pizza"]').click()`);
    await sleep(800);
    const thirdPageTheme = await evalOn(`document.documentElement.getAttribute('data-theme')`);
    const thirdPageUrl = await evalOn(`location.href`);

    ws.close();

    console.log(JSON.stringify({ initialTheme, afterToggle, secondPageTheme, secondPageUrl, thirdPageTheme, thirdPageUrl }, null, 2));

    const ok = initialTheme === 'night' && afterToggle === 'day' && secondPageTheme === 'day'
      && thirdPageTheme === 'day' && secondPageUrl.includes('RSA') && thirdPageUrl.includes('Pizza');
    if (!ok) { console.error('FAIL: theme did not survive navigation under a throwing localStorage.'); process.exit(1); }
    console.log('PASS: theme persisted across two same-tab navigations via the window.name fallback while localStorage throws.');
  } finally {
    chrome.kill();
  }
}
main().catch(e=>{ console.error('ERR', e); process.exit(1); });
NODE_EOF
)

Expected output ends with `PASS: theme persisted across two same-tab navigations via the window.name fallback while localStorage throws.`, exit code 0. This exact script was validated against the current (unfixed) repo before this plan was written — it correctly reports `secondPageTheme: "night"` / `thirdPageTheme: "night"` and exits 1 prior to this task's edit. Also re-run the same script with the `Page.addScriptToEvaluateOnNewDocument` call removed (or simply confirm manually) to prove ordinary localStorage-available persistence and the existing cross-tab `storage` event sync are unaffected — both must still work exactly as before.
    </automated>
    <human-check>In a real browser, toggle day/night on one page, click through the shared nav header to two more tools, and confirm the theme stayed selected on both destination pages. If a Safari install is available, repeat over a real `file://` URL and confirm the theme now survives navigation there too (this is the one environment that could not be exercised by headless Chrome's automated check above).</human-check>
  </verify>
  <done>`assets/theme.js` and all six pages' pre-paint scripts read `window.name` whenever `localStorage` is unavailable or empty; a simulated Safari-file:// environment (localStorage throwing) preserves the selected theme across two consecutive same-tab tool navigations; normal localStorage-based persistence and cross-tab live sync are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-local persisted theme value (localStorage / window.name) → `<html data-theme>` attribute | Client-side-only value re-read on every page load and reflected into a DOM attribute setter. No server, no other origin's data, no user-entered free text ever reaches this path. |
| Page CSS layout (`<body>` padding / `.site-header`) | Pure static-CSS relocation; no script, no data flow, no user input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-01 | Tampering | `assets/theme.js` `readTheme()` / pre-paint scripts | low | accept | Both `localStorage` and `window.name` values are validated with strict `=== 'day'` / `=== 'night'` equality before use. Any other or tampered value (including a `window.name` a prior, unrelated tab occupant may have set) silently degrades to the safe `'night'` default and is never written into `innerHTML`/`outerHTML` or evaluated as code — it only ever selects between two hardcoded literals for a single `setAttribute` call, so there is no injection surface. |
| T-01-06-02 | Information Disclosure | `window.name` persists across ALL same-tab navigations, including to origins outside this site | low | accept | The value stored is exactly the literal string `'day'` or `'night'` — no PII, no session data, no path or user information. `window.name`'s cross-origin readability by whatever page next occupies the tab is a pre-existing browser-platform property (not introduced by this fix); disclosing "this user prefers a dark or light UI" carries no meaningful risk. |
| T-01-06-03 | Denial of Service (availability regression) | `assets/theme.js`, all 6 pages' pre-paint scripts | low | mitigate | Every `localStorage` and `window.name` access remains wrapped in try/catch exactly as before, so a throwing environment (Safari file://) can only ever fall back to the safe `'night'` default, never crash page load or block rendering. Verified via this plan's Task 2 automated localStorage-throwing simulation. |
| T-01-06-04 | (no STRIDE category applies) | `body`/`.app`/`.wrap` CSS in the 4 affected HTML files | low | accept | Pure static CSS layout relocation — no script, no data flow, no user input. Logged here for completeness only; there is no elevated risk to assess. |

No package-manager installs occur in this plan (npm/pip/cargo gate not applicable).
</threat_model>

<verification>
Run both tasks' automated `<verify>` scripts from the repository root after applying both fixes. Both must print `PASS` and exit 0. Additionally, re-run 01-UAT.md's original human test (open each of the six pages, compare the header, toggle theme and navigate between tools) to confirm the two reported symptoms are gone.
</verification>

<success_criteria>
- All six pages' `.site-header` is full-width and edge-pinned with zero left offset (G-01-1a closed).
- The four previously-affected pages retain their exact original content gutter spacing.
- Theme selection survives cross-tool navigation in a simulated Safari-file:// environment (localStorage throwing), verified automatically (G-01-1b closed).
- Normal localStorage-based persistence and cross-tab sync are unaffected in Chrome/Firefox.
- Phase 01's omnibus human sign-off UAT test (01-UAT.md test 1) has no remaining reported symptoms to re-verify.
</success_criteria>

<output>
Create `.planning/phases/01-palette-unification/01-06-SUMMARY.md` when done
</output>
