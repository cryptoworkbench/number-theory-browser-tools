---
phase: quick-260926-f4v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Venn Diagrams/venn-diagrams.html
autonomous: true
requirements: [QUICK-VENN-COMPACT-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No element of the rendered page that a reader can see — the eyebrow, either lede, the h1, the picker heading and hint, the region captions, the product rows, the status message, the mode buttons, the clear button — contains any inflection of the verb the page used for co-membership (the one Task 1 retires); the replacement wording states membership through ∩ / \\ / ∪ instead (D-01)"
    - "The page eyebrow reads `intersections & set differences`; `#lede-three` explains `(A∩B) \\ C`, `A∩B∩C` and `A \\ (B ∪ C)` purely as membership statements; `#lede-two` describes `Left ∩ Right` the same way (D-01)"
    - "The JavaScript identifiers `sharedList`, `aOnly`/`bOnly`/`cOnly`, `onlyPath3`, `leftOnlyPath`, `rightOnlyPath` and the `gcd` helper are byte-identical to their pre-task state — the retired word is only removed from text a user reads, never from code (D-01)"
    - "Measured in headless Chrome at a 900x800 viewport, the full document height drops from 1441px to at most 1120px in two-circle mode and from 1839px to at most 1450px in three-circle mode (D-02, D-05)"
    - "Measured at the same viewport, the top edge of the visible diagram frame moves from 713px to at most 510px, so the chrome stacked above the diagram is cut by roughly 200px and a majority of the diagram is on screen without scrolling (D-02, D-05)"
    - "The rendered width of the visible `.diagram-frame` at a 900px-wide viewport is still at least 850px in both modes — the compaction came entirely out of the chrome, and the diagram itself was not scaled down (D-03)"
    - "Both `viewBox` attributes still read exactly `0 0 900 520` and `0 0 900 700`, and every geometry and anchor constant line (`R`/`CY`/`CXA`/`CXB`/`XM`, `R3`, `CENTERS3`, `REGION_ANCHOR`, `CHIP_ANCHOR3`, `CAPTION_ANCHOR3`) is present verbatim, so the five circle-name labels and ten region captions added by quick task 260926-eod stay exactly where their anchors put them (D-03)"
    - "At a 420x800 viewport both modes lay out with no horizontal overflow (documentElement.scrollWidth no greater than window.innerWidth + 1) and the prime chips keep a touch target of at least 44px on the narrow breakpoint (D-04)"
    - "The `<style>` block declares no literal colour value — no hex, `rgb()`/`rgba()`, `hsl()`/`hsla()` or named colour is introduced, so both themes continue to resolve every colour through `var()` against `assets/palette.css` (D-04)"
    - "The file still opens standalone from a `file://` URL with no console error, the only script carrying a src is still `../assets/theme.js`, and placing, removing, clearing, mode switching and per-mode reload persistence all still work in both modes (D-04)"
    - "The measured before/after heights for both modes are recorded in the task summary so the residual gap to a scroll-free 800px viewport is a number the user can act on, not a guess (D-05)"
  artifacts:
    - "Venn Diagrams/venn-diagrams.html"
  key_links:
    - "`.wrap` / `.page-header` / `.picker-panel` / the new `.toolbar` row / `.message` / `.products-panel` / `.product-row` padding and margin values -> the document height measured at 900x800 -> how much of the diagram and the products panel fall above the fold (D-02)"
    - "`.diagram-frame{max-width:880px}` and both `viewBox` attributes -> the rendered scale of every circle, region caption, circle-name label and placed chip -> left untouched, which is what keeps the 260926-eod anchor geometry valid (D-03)"
    - "The `#clear-btn` element moving from its own `.controls-row` wrapper into a shared `.toolbar` row beside `.mode-switch` -> one row and one margin removed from the vertical stack, while `.mode-switch` keeps its own `role=\"group\"`/`aria-label` so the mode buttons stay a labelled group (D-02, D-04)"
---

<objective>
Two follow-ups on the Prime Venn Diagram tool: retire the last plain-English co-membership verb from every string a reader sees, replacing it with set-theory phrasing consistent with the notation the page now uses everywhere else; and compact the page's vertical chrome hard — wrap padding, header margins, picker panel, the two stacked control rows, the products panel and several font sizes — so that far more of the diagram plus the products panel beneath it is visible without scrolling.

Purpose: quick task 260926-eod converted the diagram's labels, product rows and both ledes to `∩ / \ / ∪` notation, but two strings were missed and still describe membership in the old vocabulary — a small inconsistency that reads as a mistake once everything around it is notation. Separately, the page currently spends 713px of vertical space before the diagram even begins, so on a laptop viewport the reader sees prose and a prime picker while the thing the tool exists to show is below the fold.
Output: one edited file, `Venn Diagrams/venn-diagrams.html` — three reworded strings, a compacted `<style>` block, and one small markup change merging the clear button into the mode-switch row. No behaviour, no geometry, no colour changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

Target file: `Venn Diagrams/venn-diagrams.html` (1233 lines, single self-contained tool, inline `<style>` + inline IIFE `<script>`)
Prior task context: `.planning/quick/260926-eod-venn-diagrams-rename-region-labels-to-se/260926-eod-SUMMARY.md` — read the `key-decisions` and the D2 coverage entry, which record that the five circle-name labels were positioned from the geometry constants this plan must not touch.
Shared chrome (read-only reference, do not edit): `assets/site.css` — the sticky site header measures 156px tall at a 900px-wide viewport because its seven nav links wrap onto three lines.
</context>

<decisions>
Locked with the user before planning. Non-negotiable; do not re-litigate, do not simplify, do not stage into a "v1".

- **D-01** — Remove the plain-English co-membership verb from every user-visible string: the page eyebrow (currently `sets & shared factors`, line 319) and `#lede-three` (two occurrences, line 322), plus the one surviving occurrence in `#lede-two` (line 321). Reword in set-theory language consistent with the voice 260926-eod established — membership stated through `∩`, `\` and `∪`. Code identifiers (`sharedList`, `aOnly`, `onlyPath3`, …) are explicitly out of scope and must not change.
- **D-02** — Cut the page's vertical footprint by compacting chrome only: `.wrap` padding, `.page-header` margins and type sizes, `.picker-panel` padding and type, the `.mode-switch` + `.controls-row` pair (merge into one row), `.message`, `.products-panel` and `.product-row` padding, and the `.product-value` font size. Goal stated by the user: diagram and products panel both visible on a ~800px-tall viewport.
- **D-03** — Forbidden to change, because the labels added by 260926-eod were anchored against them: both `viewBox` attributes (`0 0 900 520`, `0 0 900 700`) and every geometry/anchor constant (`R`, `CY`, `CXA`, `CXB`, `XM`, `R3`, `CENTERS3`, `REGION_ANCHOR`, `CHIP_ANCHOR3`, `CAPTION_ANCHOR3`). The diagram itself must not be scaled down either: `.diagram-frame{max-width:880px}` stays, so the compaction comes out of the chrome and the diagram stays legible.
- **D-04** — Carried-over tool constraints: single self-contained file, no build step, no new dependency, no shared module; no literal colour values (everything through `var()` against `assets/palette.css`); day/night themes and narrow-width responsiveness preserved, including a touch-sized prime chip on the narrow breakpoint; the file still runs from `file://` with no console error and all interactions intact in both modes.
- **D-05** — Report measured numbers. The acceptance bar is the measured reduction at a 900x800 viewport (thresholds in Task 2), and the residual distance to a completely scroll-free 800px viewport must be recorded in the summary rather than glossed.

**Measured note on D-02 versus D-03 — read before starting Task 2, and do not silently resolve it by shrinking the diagram.** Baseline measurements taken during planning (headless Chrome, 900x800 viewport, both modes) decompose the page as: sticky site header 156px, `.wrap` padding 44+64, `.page-header` 213+30, `.mode-switch` 35+18, `.picker-panel` 144+22, `.controls-row` 35+16, diagram frame 486px (two-circle) / 654px (three-circle), `.message` 20+14, products panel 122+22 (two-circle) / 352 (three-circle) — totalling 1441px and 1839px of document height. Even with every chrome block compacted to the limit this plan allows, non-diagram content still costs roughly 570-670px, while the three-circle diagram alone wants 654px at full width: a completely scroll-free 800px viewport is therefore arithmetically unreachable without either scaling the diagram down (forbidden by D-03) or deleting whole blocks such as the lede or the prime picker (never requested). So Task 2 delivers the maximum reduction the constraints permit — measured, gated, and roughly a 200px cut to the chrome above the diagram — and reports the residual for the user's decision. Do not quietly cap `.diagram-frame`, add a `max-height` to either `svg`, or touch a `viewBox` to close the remaining gap.

<!-- planner-discipline-allow: shared -->
<!-- planner-discipline-allow: share -->
<!-- The two literals above appear in this plan's prose only because they name the text being
     removed. Task 1's acceptance gate reads element textContent out of the rendered DOM, never a
     raw grep of the source file, so a mention here cannot leak into a gate. The executor must
     still NOT write either literal into a source-code comment. -->
</decisions>

<tasks>

<task type="tracer">
  <name>Task 1: Retire the co-membership verb from every user-visible string</name>
  <files>Venn Diagrams/venn-diagrams.html</files>
  <precondition>`google-chrome-stable` is on PATH (confirmed at /usr/bin/google-chrome-stable during planning); the verify step drives it headless. Halt and report if absent.</precondition>
  <reversibility rating="reversible">Three text strings in one file, one commit.</reversibility>
  <read_first>
Read `Venn Diagrams/venn-diagrams.html` once, in full, before editing. In that single pass extract everything both tasks need: lines 319-322 (eyebrow and both ledes); the whole `<style>` block with its current padding, margin and font-size values for `.wrap`, `.page-header`, `.eyebrow`, `h1`, `.lede`, `.picker-panel`, `.picker-panel h2`, `.picker-hint`, `.prime-picker`, `.prime-chip`, `.controls-row`, `#clear-btn`, `.mode-switch`, `.mode-btn`, `.diagram-frame`, `.message`, `.products-panel`, `.product-row`, `.product-value`, and the `prefers-reduced-motion` block; the markup of the `.mode-switch` group and the `.controls-row` wrapper (lines 325-338); and the geometry constant lines Task 2 must leave untouched. Do not re-read the file afterwards — use Grep with a specific pattern if one detail is missing.
  </read_first>
  <action>
Three string edits in the markup, nothing else. Do not touch the `<script>` block at all in this task, and do not write the retired word into a comment anywhere.

Eyebrow (line 319): replace the current text with `intersections &amp; set differences`. Keep the `<p class="eyebrow">` element, the entity style for the ampersand, and the all-lowercase voice the CSS uppercases.

`#lede-two` (line 321): keep the first sentence exactly as it is (the one about two circles being two numbers written as prime factorizations). In the second sentence, restate the middle region as a membership fact rather than an act of co-ownership: `Left ∩ Right` holds the primes that belong to both factorizations, so that intersection multiplies out to the largest number dividing both, while `Left \ Right` and `Right \ Left` hold the primes belonging to just one of them. Preserve the `&mdash;`, the `class`, the `id`, and the literal backslash and `∩` glyphs (this is HTML source, so no escaping is needed here — the doubling rule applies only to JS string literals).

`#lede-three` (line 322): keep the first sentence. Rewrite the three clauses as membership statements: a prime in `(A∩B) \ C` belongs to A and B but not C, so that whole lens multiplies out to `A ∩ B`; a prime in `A∩B∩C` belongs to all three at once, so the centre multiplies out to `A ∩ B ∩ C`; a prime in `A \ (B ∪ C)` belongs to A alone. Keep the trailing `hidden` attribute and the `id`.

Then sweep the rest of the rendered surface and fix anything else that reads in the retired vocabulary: the `h1`, the picker heading and hint, the mode-button and clear-button labels, the `formatSegments` segment notes (`with A` / `with B` / `with C` / `all three`) and the status-message sentences in `placePrime`/`placePrime3`/`removeToken`/`removeToken3`/`clearAll`/`toggleArm`. The planning sweep found no further occurrence in any of them, so the expected outcome of this sweep is no additional edit — confirm that rather than inventing changes, and specifically leave the segment notes alone: the user ruled them acceptable in 260926-eod and lengthening them into notation would push the product rows into extra wrapping that Task 2 is trying to remove.
  </action>
  <verify>
    <automated>Dump both modes, from the repo root: `REPO=/home/mainaccount/Claude/number-theory-browser-tools; SP="$SCRATCH"; U="file://$REPO/Venn%20Diagrams/venn-diagrams.html"; for M in two three; do google-chrome-stable --headless --disable-gpu --no-sandbox --user-data-dir="$(mktemp -d)" --virtual-time-budget=4000 --dump-dom "$U?mode=$M" > "$SP/dom-$M.html"; done` — then assert with `grep -F -q` against `$SP/dom-two.html`: `>intersections &amp; set differences<`, `Left ∩ Right holds the primes that belong to both factorizations`, `belongs to A and B but not C`, `belongs to all three at once`, `belongs to A alone`. Exit non-zero on the first miss.</automated>
    <automated>Element-scoped vocabulary gate — run node over the dump so the inline script's identifiers cannot be mistaken for rendered text: extract the textContent of every `.eyebrow`, `.lede`, `h1`, `.picker-panel h2`, `.picker-hint`, `.mode-btn`, `#clear-btn`, `#message`, `text.region-label` and `span.product-value` node from `$SP/dom-two.html` and `$SP/dom-three.html`, assert the extraction found at least 20 nodes per dump (so a dropped selector fails loudly rather than passing vacuously), and assert that none of the collected strings matches `/shar(e|es|ed|ing)/i`. Print `PASS <nodeCount>` and exit non-zero on any match.</automated>
    <automated>Identifier-preservation gate: `grep -c 'sharedList' "$REPO/Venn Diagrams/venn-diagrams.html"` returns 4, `grep -c 'onlyPath3' "$REPO/Venn Diagrams/venn-diagrams.html"` returns 4, and `git -C "$REPO" diff --numstat -- "Venn Diagrams/venn-diagrams.html"` shows exactly 3 lines added and 3 lines removed — proving the edit touched only the three prose lines and no code.</automated>
  </verify>
  <done>The eyebrow reads `intersections & set differences`, both ledes state membership in `∩` / `\` / `∪` terms, no user-visible string in either mode matches the retired verb, the JS identifiers and the diff size prove no code was touched, and a headless dump of both modes confirms it in the rendered DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Compact the page chrome and prove the diagram is untouched</name>
  <files>Venn Diagrams/venn-diagrams.html</files>
  <precondition>`google-chrome-stable` is on PATH; every gate in this task is a headless layout measurement. Halt and report if absent.</precondition>
  <reversibility rating="reversible">CSS values plus one wrapper-element change in a single file; revert is one commit.</reversibility>
  <action>
Build the measurement harness first, because every acceptance number in this task comes from it, then edit, then measure.

**Harness** (session scratchpad only — never write a temp file into the repo, and never commit one). Write `$SCRATCH/measure-venn.sh` taking three arguments: max document height for two-circle mode, max for three-circle mode, max diagram-frame top. It must: copy `Venn Diagrams/venn-diagrams.html` into the scratchpad; rewrite the three `../assets/…` references (`palette.css`, `site.css`, `theme.js`) to absolute `file://` paths so the copy styles identically; append before `</body>` a script registering its own `load` listener (it therefore runs after the tool's) that, after a 400ms settle, appends a `div#measure-out` whose text carries `DOCH=<documentElement.scrollHeight> FRAMETOP=<round(rect.top+scrollY) of the visible .diagram-frame> FRAMEW=<round(rect.width) of that frame> SCROLLW=<documentElement.scrollWidth> INNERW=<window.innerWidth> PRODBOTTOM=<round(rect.bottom+scrollY) of the last visible .products-panel>`; drive `google-chrome-stable --headless --disable-gpu --no-sandbox` with a fresh `--user-data-dir` and `--virtual-time-budget=4000` at `--window-size=900,800` for `?mode=two` and `?mode=three`, and again at `--window-size=420,800` for both modes; print one labelled line per run; and exit non-zero if any of the three thresholds is exceeded or if `SCROLLW` exceeds `INNERW+1` in either 420px-wide run. Run it once before editing and record the baseline it reports — planning measured `DOCH` 1441 / 1839, `FRAMETOP` 713 / 713, `FRAMEW` 856, `PRODBOTTOM` 1377 / 1775, and a disagreement of more than a few pixels means the harness is measuring something other than the real page and must be fixed before the edits are judged by it.

**Edits.** All inside the existing `<style>` block except the one markup change. Change no colour, add no custom property, add no new selector beyond `.toolbar`, and leave `.diagram-frame`, both `svg` rules, every `viewBox` and the whole `<script>` block alone (D-03).

- `.wrap`: padding `44px 22px 64px` becomes `20px 22px 28px`. The sticky site header is in normal flow, so the top padding is not header compensation and is safe to cut.
- `.page-header`: `margin-bottom` 30px becomes 14px, and `max-width` 68ch becomes 92ch — the wider measure is deliberate, it is what takes the lede from five rendered lines to three.
- `.eyebrow`: `font-size` 12px becomes 11.5px, `margin` bottom 12px becomes 6px.
- `h1`: `font-size` becomes `clamp(24px, 3.2vw, 34px)`, `margin` bottom 14px becomes 8px. Keep `line-height`, `text-wrap` and `letter-spacing`.
- `.lede`: `font-size` 16px becomes 14.5px, `line-height` 1.6 becomes 1.45.
- Merge the two control rows into one. In the markup, wrap the existing `.mode-switch` div and the `#clear-btn` button in a new `<div class="toolbar">`, delete the now-empty `.controls-row` wrapper, and keep `.mode-switch`'s `role="group"` and `aria-label="Diagram mode"` on the mode-switch div itself so the two mode buttons remain a labelled group and the clear button stays outside it. In CSS, delete the `.controls-row` rule, add `.toolbar{ display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap; margin-bottom:12px; }` and set `.mode-switch{ margin-bottom:0; }` (keep its `display:flex` and `gap`, reduce the gap to 6px).
- `.picker-panel`: padding `18px 22px` becomes `10px 14px`, `margin-bottom` 22px becomes 12px. `.picker-panel h2`: `font-size` 17px becomes 15px, bottom margin 6px becomes 4px. `.picker-hint`: `font-size` 13.5px becomes 12.5px, bottom margin 14px becomes 8px. `.prime-picker`: `gap` 8px becomes 6px. `.prime-chip`: `width` 46px becomes 40px, `height` 46px becomes 38px, `font-size` 15px becomes 14px.
- Protect the touch target on small screens (D-04): add a `@media (max-width: 560px)` block restoring `.prime-chip{ width:44px; height:44px; }`. Place it next to the existing `prefers-reduced-motion` block.
- `.message`: `min-height` 20px becomes 16px (keep the property — it is what stops the layout jumping when a message appears), `font-size` 14px becomes 13.5px, `margin` `14px 0 0` becomes `8px 0 0`.
- `.products-panel`: `gap` 14px becomes 10px, `margin-top` 22px becomes 12px. `.product-row`: padding `14px 16px` becomes `8px 10px`. `.product-value`: `font-size` 19px becomes 15px, padding `8px 6px` becomes `6px 6px`, and add `line-height:1.35` — the product strings wrap to several lines inside their card, so the font size and line box are the dominant term in the three-circle products panel's 352px.

Re-run the harness after the edits. If a threshold still fails, take further reductions from the same chrome list — never from `.diagram-frame`, an `svg` rule, a `viewBox`, or by deleting the lede or the picker. Record the final measured `DOCH`, `FRAMETOP` and `PRODBOTTOM` for both modes in the task summary alongside the baseline, and state plainly how far the three-circle mode still is from a scroll-free 800px viewport (D-05).
  </action>
  <verify>
    <automated>`bash "$SCRATCH/measure-venn.sh" 1120 1450 510` exits 0 — asserting document height at most 1120px in two-circle mode (from 1441) and at most 1450px in three-circle mode (from 1839), diagram-frame top at most 510px (from 713), and no horizontal overflow in either 420px-wide run.</automated>
    <automated>No-shrink gate: the same harness output reports `FRAMEW` of at least 850 for both 900px-wide runs, proving the diagram still renders at essentially its full 856px and the compaction came out of the chrome (D-03).</automated>
    <automated>Geometry-preservation gate, from the repo root, each `grep -F -c` returning exactly 1 against `Venn Diagrams/venn-diagrams.html`: `viewBox="0 0 900 520"`, `viewBox="0 0 900 700"`, `var R = 190, CY = 250, CXA = 355, CXB = 545, XM = 450;`, `var R3 = 190;`, `var REGION_ANCHOR = { left: [275, 250], overlap: [450, 250], right: [625, 250] };`, `aOnly: [281, 232], bOnly: [619, 232], cOnly: [450, 520],`, `aOnly: [281, 152], bOnly: [619, 152], cOnly: [450, 600],`, `{ x: 450, y: 450 }`. Also assert `git -C /home/mainaccount/Claude/number-theory-browser-tools diff -- "Venn Diagrams/venn-diagrams.html"` contains no changed line matching `CENTERS3|CHIP_ANCHOR3|CAPTION_ANCHOR3|viewBox|diagram-frame|svg#venn`.</automated>
    <automated>Label-integrity gate: re-dump both modes and re-run Task 1's element extraction, asserting it still finds exactly 10 `text.region-label` nodes and exactly 5 `text.circle-name` nodes with texts `Left`, `Right`, `A`, `B`, `C`, and exactly 10 `span.product-value` nodes — the 260926-eod labels survived the restyle intact.</automated>
    <automated>Palette gate (baseline is zero, so any hit is new): `grep -nEi 'rgba?\(|hsla?\(|#[0-9a-f]{3}([0-9a-f]{3})?\b' "Venn Diagrams/venn-diagrams.html" | grep -vE '/\*|<!--' | wc -l` returns 0. Dependency gate: `grep -c 'script.*src' "Venn Diagrams/venn-diagrams.html"` returns exactly 1 and the matching line references `../assets/theme.js`.</automated>
    <automated>Regression sweep: four headless screenshots with a fresh `--user-data-dir` at `--window-size=900,800` — `?theme=day&mode=two`, `?theme=night&mode=two`, `?theme=day&mode=three`, `?theme=night&mode=three` — all four non-empty; and both modes loaded with `--enable-logging=stderr --log-level=0` produce no `ERROR:CONSOLE` line.</automated>
    <human-check>Review the four screenshots, then drive the live page: the header, picker and toolbar read as compact rather than cramped; the diagram and its labels are unchanged in size and placement; product rows still fit their cards at the smaller font (wrapping acceptable, clipping not); place, remove and clear primes in both modes; switch modes; reload and confirm each mode restores its own placements; narrow the window to phone width and confirm the chips stay comfortably tappable and nothing overflows sideways.</human-check>
  </verify>
  <done>The measured document height at a 900x800 viewport is at most 1120px in two-circle mode and 1450px in three-circle mode, the diagram frame starts at or above 510px, the diagram still renders at full width with every `viewBox` and geometry constant verbatim and all fifteen SVG labels intact, no colour literal or dependency was added, narrow widths and both themes still render cleanly with no console error, and the summary records the measured before/after numbers plus the residual gap to a scroll-free 800px viewport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → DOM | Stored payloads under `venn-diagrams`, `venn-diagrams-three` and `venn-diagrams-mode` are writable by any script on the same origin (or by hand on a `file://` origin) and are read back on load. Untouched by this plan. |
| URL query string → DOM | The `?mode=` and `?theme=` parameters are parsed at load. Untouched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-f4v-01 | Tampering | `restore`/`restore3` reading `localStorage` | low | accept | Pre-existing path, not touched by this plan; both restores already validate array shape, length cap and primality per entry and fall back to the seed, and values reach the DOM only via `textContent`. |
| T-f4v-02 | Tampering | `readModeParam` reading `?mode=` | low | accept | Pre-existing path, not touched by this plan; the value is whitelisted to `two`/`three` before use. |
| T-f4v-03 | Information disclosure | Injected measurement harness | low | mitigate | The harness lives only in the session scratchpad, references the tool by absolute `file://` path, and is never written into the repo or committed — Task 2 states this explicitly and the geometry gate's `git diff` check would catch a stray temp file in the diff. |

No package-manager install occurs in this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- Task 1's headless dump proves the retired vocabulary is gone from every user-visible node in both modes, while the identifier and diff-size gates prove no code moved.
- Task 2's harness is the single source of truth for the compaction claim: it measures the real file (a styled copy of it) rather than reasoning about CSS, and it fails closed on document height, chrome height above the diagram, diagram width and narrow-width overflow.
- The geometry-preservation and label-integrity gates together protect the 260926-eod work: the constants are verbatim, the frame is full width, and all fifteen SVG label nodes are still present with their expected text.
- Palette, dependency, console-error and both-theme screenshot gates protect the standing project constraints.
</verification>

<success_criteria>
- No string a reader can see on the page uses the retired co-membership verb; the eyebrow and both ledes read as set-theory prose consistent with the diagram's notation.
- Document height at a 900x800 viewport: at most 1120px in two-circle mode (from 1441px) and at most 1450px in three-circle mode (from 1839px); the diagram frame begins at or above 510px (from 713px).
- Diagram rendered width, both `viewBox` attributes, every geometry and anchor constant, and all fifteen SVG labels are unchanged.
- Narrow-width layout, both themes, touch-sized chips on the small breakpoint, zero colour literals, one external script, no console error, and every interaction in both modes still working.
- The summary records measured before/after heights and the residual gap to a scroll-free 800px viewport.
</success_criteria>

<output>
Create `.planning/quick/260926-f4v-venn-diagrams-tool-follow-up-1-eliminate/260926-f4v-SUMMARY.md` when done
</output>
