---
phase: quick-260926-g4b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Venn Diagrams/venn-diagrams.html
autonomous: true
requirements: [QUICK-VENN-AB-LABELS-01]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No string a reader can see anywhere on the page names a circle by its screen position: `grep -cE '\\b(Left|Right)\\b' \"Venn Diagrams/venn-diagrams.html\"` returns 0, down from the 6 occurrences measured during planning at lines 329, 479, 662, 666, 1056 and 1061 (D-01)"
    - "In two-circle mode the two persistent circle-name labels read `A` and `B`, the three region captions read `A \\ B`, `A ∩ B` and `B \\ A`, and every status message and region `aria-label` names regions with those same strings, because all of them are generated from the single `REGION_NAMES` dictionary (D-01)"
    - "Every structural identifier that encodes left/right *position* rather than a set *name* is byte-identical to its pre-task state: CSS classes `.circle-left`/`.circle-right`, state keys `state.regions.left`/`.right`, element ids `product-left`, `product-right`, `region-left`, `region-right`, `circle-name-left`, `circle-name-right`, functions `leftOnlyPath`/`rightOnlyPath`, the `localStorage` payload keys `left`/`right`, and the variables `productLeftEl`, `productRightEl`, `leftTotal`, `rightTotal`, `leftOnlyList`, `rightOnlyList`, `rowLeft` (D-03)"
    - "Every info box under the diagram is a single short arithmetic line with no parenthetical region notes, no `(empty)` placeholder, and no prose: a side box reads `A = 2 * 2 * 5 = 20`, an intersection box reads `A ∩ B = 20 ∩ 70 = 10 = 2 * 5` (D-02)"
    - "Loaded with the user's worked example seeded (A-only `2`, lens `2, 5`, B-only `7`) the three two-circle boxes read exactly `A = 2 * 2 * 5 = 20`, `B = 2 * 5 * 7 = 70` and `A ∩ B = 20 ∩ 70 = 10 = 2 * 5` — the verbatim output the user asked for (D-02, D-05)"
    - "Loaded with the shipped default seed the three two-circle boxes read exactly `A = 2 * 3 * 5 = 30`, `B = 5 * 7 = 35`, `A ∩ B = 30 ∩ 35 = 5`, and the seven three-circle boxes read exactly `A = 2 * 7 * 11 * 17 = 2618`, `B = 3 * 7 * 13 * 17 = 4641`, `C = 5 * 11 * 13 * 17 = 12155`, `A ∩ B = 2618 ∩ 4641 = 119 = 7 * 17`, `A ∩ C = 2618 ∩ 12155 = 187 = 11 * 17`, `B ∩ C = 4641 ∩ 12155 = 221 = 13 * 17`, `A ∩ B ∩ C = 2618 ∩ 4641 ∩ 12155 = 17` (D-02)"
    - "Degenerate inputs still print true arithmetic and never a stray operator: an empty circle prints `A = 1`, a one-prime circle prints `A = 7` with no redundant `= 7` tail, and a coprime pair prints its intersection as `… = 1` with no factorization tail (D-02)"
    - "Every factorization tail multiplies to the value printed immediately before it — asserted numerically, not eyeballed — so no box can display an arithmetically false line (D-02, D-05)"
    - "The now-unreferenced `formatSegments` helper is deleted: `grep -c formatSegments \"Venn Diagrams/venn-diagrams.html\"` returns 0, and `gcd`, `productOf`, `isPrime`, `primesOf`, `primesOf3` are unchanged (D-02, D-03)"
    - "The `<style>` block declares no literal colour value — no hex, `rgb()`/`rgba()`, `hsl()`/`hsla()` or named colour is introduced, so both themes still resolve every colour through `var()` against `assets/palette.css` (D-04)"
    - "The file still opens standalone from a `file://` URL with no console error, the only script carrying a `src` is still `../assets/theme.js`, and placing, removing, clearing, mode switching and per-mode reload persistence all still work in both modes (D-04)"
    - "A human has loaded the real file in a real browser, exercised both modes, and confirmed by eye that no user-facing positional set name remains and that the boxes read as short arithmetic (D-05)"
  artifacts:
    - "Venn Diagrams/venn-diagrams.html"
  key_links:
    - "`REGION_NAMES` (line 479) -> the three SVG region captions in `buildStatic`, the `aria-label` built by `regionAriaLabel`, the four status-message sentences in `placePrime`/`removeToken`, and the middle info box's label -> renaming one dictionary renames the whole two-circle surface at once (D-01)"
    - "the new `factorize(n)` helper -> the ` = f1 * f2` tail on every intersection box -> must factor the *computed gcd*, not the lens contents, because `gcd(totalA, totalB)` can legitimately exceed the product of the lens primes when the same prime sits in both outer regions (D-02)"
    - "deleting `formatSegments` -> both `renderProducts` and `renderProducts3` must already be rewritten, or the page throws on load and every box goes blank (D-02)"
    - "`state.regions.left`/`.right` and the `localStorage` payload keys -> a previously saved diagram must still restore after the rename; renaming those keys would silently discard every user's stored state (D-03)"
---

<objective>
Two changes to the Prime Venn Diagram tool, both inside the one existing file. First, rename the set that the page currently calls by its screen position to `A`, and the other to `B`, everywhere those words function as the set's *name* — the persistent circle labels, the region captions, the region `aria-label`s, the status messages, the info-box row prefixes and the two-circle lede. Second, replace the info boxes under the diagram with short arithmetic lines: each side box shows that circle's own prime factors multiplied out to its number, and each intersection box shows the two (or three) numbers intersected down to the shared value with its factorization.

Purpose: the three-circle mode has called its sets `A`, `B`, `C` since it shipped, while the two-circle mode still calls the same two circles by where they sit on screen — so the same tool speaks two different languages depending on which button is pressed, and the two-circle boxes read as a paragraph of set-theory notation rather than the one line of arithmetic a self-learner actually wants. The user's own words for the target: "keep this short and simplistic."
Output: one edited file, `Venn Diagrams/venn-diagrams.html` — a renamed `REGION_NAMES` dictionary, two renamed circle labels, one reworded lede, both product renderers rewritten around a new `factorize()` helper, and the now-dead `formatSegments` helper deleted. No new file, no new dependency, no CSS change, no geometry change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

Target file: `Venn Diagrams/venn-diagrams.html` (1240 lines; single self-contained tool, inline `<style>` + inline IIFE `<script>`, the only `src` script is `../assets/theme.js`).
Prior task context: `.planning/quick/260926-eod-venn-diagrams-rename-region-labels-to-se/260926-eod-SUMMARY.md` — that task introduced the five persistent circle-name labels and the `x ∩ y = value` infix this task builds on, and deleted a `formatSide` helper once it went unreferenced (the same precedent this plan applies to `formatSegments`).
Prior task context: `.planning/quick/260926-ckc-fix-venn-diagrams-tool-overlap-region-sh/260926-ckc-SUMMARY.md` — that task is where the middle row became a `gcd` of the two circle totals rather than a product of the lens contents; read it before touching the intersection rows.
Shared chrome (read-only reference, do not edit): `assets/palette.css`, `assets/site.css`, `assets/theme.js`.

**Modes the tool actually has (planning finding — do not design for a mode that does not exist).** The user's brief speculated the tool might have a plain-set-elements mode alongside a prime-factorization mode. It does not. The two modes are **Two circles** and **Three circles** (`#mode-two` / `#mode-three`, persisted under `venn-diagrams-mode`, also settable with `?mode=two|three`), and *both* are prime-factorization modes: every token placed in a region is a prime from a 12-prime picker, and every box multiplies primes out to a number. So the arithmetic-style box format specified in D-02 applies to both modes, and no `A = {1, 2, 3}` element-listing format is needed anywhere. Say so in the summary rather than leaving the user's hypothesis hanging.
</context>

<decisions>
Derived from the user's verbatim brief and the orchestrator's constraints. Non-negotiable; do not re-litigate, do not simplify, do not stage into a "v1".

- **D-01** — Rename the set currently labelled by its screen position to `A`, and the other to `B`, **everywhere the word functions as the set's name**, not merely in one display string. Planning enumerated the complete set of six source lines: the `#lede-two` prose (line 329); the `REGION_NAMES` dictionary values (line 479) which feed the three SVG region captions, the region `aria-label`s and four status-message sentences; the two `textContent` assignments for the persistent circle labels (lines 662, 666); and the two info-box row prefixes (lines 1056, 1061). `REGION_NAMES` becomes `{ left: 'A \\ B', overlap: 'A ∩ B', right: 'B \\ A' }`.
- **D-02** — Replace the info boxes under the diagram with short arithmetic lines in both modes. Remove the parenthetical region notes, the `with B` / `with C` / `all three` notes and the `(empty)` placeholder entirely. A side box shows that circle's own prime factors and its product; an intersection box shows both (or all three) circle totals intersected down to the shared value, followed by that value's factorization. Exact formats and degenerate-case rules are spelled out in Task 2 and must be followed to the character.
- **D-03** — Identifiers that encode left/right **position** rather than a set **name** are explicitly out of scope and must not change: the CSS classes `.circle-left`/`.circle-right`, the state keys `state.regions.left`/`.right`, the element ids `product-left`/`product-right`/`region-left`/`region-right`/`circle-name-left`/`circle-name-right`, the path builders `leftOnlyPath`/`rightOnlyPath`, the `localStorage` payload keys, and every camelCase variable containing `left`/`right`. The `localStorage` keys in particular: renaming them would silently discard every existing user's saved diagram.
- **D-04** — Carried-over tool constraints: one self-contained file edited in place, no new file, no build step, no new dependency, no shared JS module; no literal colour value anywhere (every colour through `var()` against `assets/palette.css` — this task is text and logic, so the expected number of new colour declarations is zero, and the executor must confirm that rather than assume it); day/night themes and both modes keep working; the file still runs from a `file://` URL with no console error; placing, removing, clearing, mode switching and per-mode reload persistence all survive.
- **D-05** — This repo has no test suite, so correctness is proved by rendering. Every acceptance gate in this plan drives headless Chrome over the real file (or a scratchpad copy of it with a different seed) and asserts exact box strings; on top of that a human loads the file in a real browser and confirms by eye. At minimum the user's own worked example must be reproduced verbatim and reported in the summary.

**Ordering note on the factorization tail — read before writing Task 2, and do not "fix" it.** The middle box has printed `gcd(totalA, totalB)` since quick task 260926-ckc, and `totalA`/`totalB` include the lens primes on both sides. That means the printed intersection value is *not* always the product of the lens contents: place the same prime in both outer regions with an empty lens and the tool prints `A ∩ B = 3 ∩ 3 = 3` while the lens looks empty. That is a pre-existing property of the tool's gcd framing, not a bug this task introduces, and it is out of scope. The consequence for D-02 is concrete and mandatory: the factorization tail must come from factoring **the computed gcd value**, never from re-listing the lens contents — that is what keeps the printed line internally true in the degenerate case. Task 3 probes exactly this case and records the observation in the summary for the user's own decision; do not change the gcd framing, the region model, or `gcd()` to make the lens and the value agree.

<!-- planner-discipline-allow: Left -->
<!-- planner-discipline-allow: Right -->
<!-- The two literals above are allow-listed because Task 1's acceptance gate negative-greps the
     SOURCE FILE for them, and Task 1's action cannot be actionable without naming the exact
     strings it replaces. They appear in this plan only. The executor must NOT write either word
     into a comment, a string, or any other line of `Venn Diagrams/venn-diagrams.html` — a single
     such comment would make the Task 1 gate fail, and correctly so. -->
</decisions>

<tasks>

<task type="tracer">
  <name>Task 1: Rename the positional set names to A and B across the whole two-circle surface</name>
  <files>Venn Diagrams/venn-diagrams.html</files>
  <precondition>`google-chrome-stable` is on PATH (confirmed at `/usr/bin/google-chrome-stable` during planning) and `node` is on PATH (confirmed at `/usr/bin/node`); every gate in this plan drives them. Halt and report if either is absent.</precondition>
  <reversibility rating="reversible">Six string edits in one file; revert is one commit.</reversibility>
  <read_first>
Read `Venn Diagrams/venn-diagrams.html` **once, in full**, before editing anything. In that single pass extract everything all three tasks need, because re-reading the file is forbidden: line 329 (`#lede-two`); line 479 (`REGION_NAMES`) and lines 480-481; `buildStatic` at 644-668 (the three region captions and the two circle-name labels, with their `x`/`y` anchors and their `name-a`/`name-b` classes); `regionAriaLabel` at 787-791; the four message sentences in `placePrime` (816, 820) and `removeToken` (830); `formatSegments` at 436-448; `productOf` at 419-423 and `gcd` at 425-434; `renderProducts` at 1048-1065; `renderProducts3` at 1108-1153; `REGION_NAMES3` at 568-576; the product element lookups at 474-476 and 602-610; the markup of both products panels at 363-401; and the load-time seed block at 1213-1235. If one detail turns out to be missing later, use Grep with a specific pattern — do not re-Read a range already in context.
  </read_first>
  <action>
Six edits, all of them strings. Touch nothing else in this task — no renderer logic, no helper, no CSS, no geometry, no element id, no state key (D-03). Per D-01 and the allow-list note in `<decisions>`, do not write either replaced word anywhere in the file, including in a comment.

1. **Line 479, `REGION_NAMES`** — replace the three values so the dictionary reads exactly `var REGION_NAMES = { left: 'A \\ B', overlap: 'A ∩ B', right: 'B \\ A' };`. Keep the three object keys (`left`, `overlap`, `right`) exactly as they are: they are positional state keys, not names (D-03). Note this is a JS string literal, so the set-difference backslash stays doubled, exactly as it is today.

2. **Line 662** — `nameLeft.textContent` becomes `'A'`. Leave the variable name `nameLeft`, the element id `circle-name-left`, the `name-a` class and the `x: 325, y: 100` anchor untouched.

3. **Line 666** — `nameRight.textContent` becomes `'B'`. Leave the variable name `nameRight`, the element id `circle-name-right`, the `name-b` class and the `x: 575, y: 100` anchor untouched. The `name-a`/`name-b` classes already colour these two labels with `--role-input` and `--role-alt`, which is exactly the A/B colour pairing the three-circle mode uses — so no CSS change is needed or wanted here (D-04).

4. **Line 1056** — the row prefix string becomes `'A = '`. Leave the `formatSegments(...)` call alone in this task; Task 2 replaces the whole body of `renderProducts`.

5. **Line 1061** — the row prefix string becomes `'B = '`.

6. **Line 329, `#lede-two`** — swap the four positional names for `A` and `B`, and name the circles once in the opening clause so the labels on the diagram are introduced rather than assumed. Keep the sentence structure, the `&mdash;`, the `class` and the `id`. The result: the two circles are two numbers A and B, each written out as its prime factorization — everything inside a circle multiplies to that number; `A ∩ B` holds the primes that belong to both factorizations, so that intersection multiplies out to the largest number dividing both, while `A \ B` and `B \ A` hold the primes belonging to just one of them. This is HTML source, not a JS literal, so the backslash and `∩` glyphs are written plainly with no escaping.

Then sweep for anything the enumeration missed. The `<title>`, the `.eyebrow`, the `h1`, the two `svg` `aria-label` attributes, the mode-button labels, the clear-button label, the picker heading and the `#lede-three` prose were all checked during planning and contain no positional set name — so the expected outcome of this sweep is no additional edit. Confirm that rather than inventing changes.
  </action>
  <verify>
    <automated>Source gate, from the repo root: `grep -cE '\b(Left|Right)\b' "Venn Diagrams/venn-diagrams.html"` returns `0` (planning measured 6). The `\b` word boundaries are what make this gate correct rather than over-broad: they do not match `productLeftEl`, `labelLeft`, `nameRight`, `leftOnlyList`, `rowLeft`, `leftTotal` or any other camelCase identifier, and they do not match the lowercase `left`/`right` in CSS classes, state keys or element ids — verified during planning, where this exact pattern matched only the six lines this task edits.</automated>
    <automated>Identifier-preservation gate, from the repo root, each command returning the stated count against `Venn Diagrams/venn-diagrams.html`: `grep -c 'circle-left'` returns 2, `grep -c 'circle-right'` returns 2, `grep -c 'leftOnlyPath'` returns 2, `grep -c 'rightOnlyPath'` returns 2, `grep -c 'state.regions.left'` returns 4, `grep -c 'state.regions.right'` returns 4, `grep -Fc "id: 'circle-name-left'"` returns 1, `grep -Fc "id: 'circle-name-right'"` returns 1. Then `git -C . diff --numstat -- "Venn Diagrams/venn-diagrams.html"` shows exactly 6 lines added and 6 lines removed — proving this task touched only the six string lines (D-03).</automated>
    <automated>Rendered gate. Dump both modes into a fresh temp dir: `SP="$(mktemp -d)"; U="file://$PWD/Venn%20Diagrams/venn-diagrams.html"; for M in two three; do google-chrome-stable --headless=new --disable-gpu --no-sandbox --user-data-dir="$(mktemp -d)" --virtual-time-budget=4000 --dump-dom "$U?mode=$M" > "$SP/dom-$M.html"; done`. Then with `node` over `$SP/dom-two.html`, extract the `textContent` of every `text.circle-name`, `text.region-label` and `span.product-value` node and assert: exactly 5 `text.circle-name` nodes whose texts are `A`, `B`, `A`, `B`, `C` in document order (two from the two-circle svg, three from the three-circle svg — both svgs live in the same document, so duplicates across them are expected); exactly 10 `text.region-label` nodes, of which the first three are exactly `A \ B`, `A ∩ B` and `B \ A`; exactly 10 `span.product-value` nodes; and that none of the collected strings matches `/\b(Left|Right)\b/`. Assert the node counts explicitly so a dropped selector fails loudly instead of passing vacuously. Print `PASS <total node count>`; exit non-zero on any mismatch.</automated>
    <automated>Lede gate: `grep -F -q 'A ∩ B holds the primes that belong to both factorizations' "Venn Diagrams/venn-diagrams.html"` succeeds, and `node` extracting `#lede-two`'s `textContent` from `$SP/dom-two.html` finds no match for `/\b(Left|Right)\b/` and does find `A \ B` and `B \ A`.</automated>
  </verify>
  <done>`grep -cE '\b(Left|Right)\b'` over the source returns 0; the rendered DOM of both modes shows circle labels `A`/`B`, region captions `A \ B`, `A ∩ B`, `B \ A`, and no positional set name in any caption, label or box; every positional identifier and both `localStorage` payload key sets are byte-identical; and the diff is exactly 6 lines changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite both info-box renderers as short arithmetic lines</name>
  <files>Venn Diagrams/venn-diagrams.html</files>
  <precondition>Task 1 is committed — its `REGION_NAMES.overlap` value (`A ∩ B`) is the label this task reuses for the two-circle intersection box, so running this task first would print the old positional name.</precondition>
  <reversibility rating="reversible">Two render functions plus one new helper in a single file; revert is one commit.</reversibility>
  <behavior>
Write the assertion script **before** editing the file, run it against the current file and watch it fail (red), then implement, then watch it pass (green). The script drives headless Chrome over two seeds and asserts exact strings. These are the expected strings — every one of them was computed and cross-checked against a real headless dump of the current file during planning, so the intersection values (5, 119, 187, 221, 17, 10) are the tool's own `gcd()` output, not a guess.

**Seed U — the user's worked example** (a scratchpad copy of the file with the two-circle seed replaced by: A-only `[2]`, lens `[2, 5]`, B-only `[7]`). The three two-circle boxes must read, character for character:
  - `A = 2 * 2 * 5 = 20`
  - `B = 2 * 5 * 7 = 70`
  - `A ∩ B = 20 ∩ 70 = 10 = 2 * 5`

**Seed D — the shipped default seed, the unmodified repo file** (two-circle: A-only `[2, 3]`, lens `[5]`, B-only `[7]`; three-circle: A-only `[2]`, B-only `[3]`, C-only `[5]`, AB `[7]`, AC `[11]`, BC `[13]`, ABC `[17]`). All ten boxes must read, character for character:
  - `A = 2 * 3 * 5 = 30`
  - `B = 5 * 7 = 35`
  - `A ∩ B = 30 ∩ 35 = 5`
  - `A = 2 * 7 * 11 * 17 = 2618`
  - `B = 3 * 7 * 13 * 17 = 4641`
  - `C = 5 * 11 * 13 * 17 = 12155`
  - `A ∩ B = 2618 ∩ 4641 = 119 = 7 * 17`
  - `A ∩ C = 2618 ∩ 12155 = 187 = 11 * 17`
  - `B ∩ C = 4641 ∩ 12155 = 221 = 13 * 17`
  - `A ∩ B ∩ C = 2618 ∩ 4641 ∩ 12155 = 17`

Note what the two seeds jointly pin down: a multi-factor side box carries a `= product` tail (`2 * 3 * 5 = 30`); a multi-factor intersection value carries a `= factorization` tail (`119 = 7 * 17`); a single-prime intersection value carries **no** tail (`= 5`, `= 17`), because repeating it would be noise. Note also that `A = 2 * 2 * 5 = 20` proves the factor list is a multiset printed in ascending order, not a deduplicated set — a repeated prime must print twice.
  </behavior>
  <action>
Build the probe harness first (it is this task's red/green instrument), then edit, then re-run it.

**Harness** — session scratchpad only, via `SP="$(mktemp -d)"`. Never write a temp file into the repo and never commit one. It must: copy `Venn Diagrams/venn-diagrams.html` into `$SP`; rewrite the three `../assets/…` references (`palette.css`, `site.css`, `theme.js`) to absolute `file://` paths so the copy styles identically; for a named seed, replace the three `state.regions.*` assignment lines inside the `if (!restore()){ … }` block with that seed's values **and** change that condition to `if (true)` so a stored diagram can never win; drive `google-chrome-stable --headless=new --disable-gpu --no-sandbox` with a **fresh `--user-data-dir="$(mktemp -d)"` per run** and `--virtual-time-budget=4000`, once per mode via `?mode=two` and `?mode=three`; and pipe each `--dump-dom` through `node`, which extracts the `textContent` of all ten `span.product-value` nodes in document order and compares them to the expected list with strict equality, printing the first mismatch as `expected <x> | got <y>` and exiting non-zero. Run it against the unmodified file first: the expected result is a failure on the very first string, which confirms the harness is really reading the boxes and not passing vacuously.

**Edits.** Three changes to the `<script>` block, nothing else. No CSS, no markup, no element id, no state key, no geometry, and no change to `isPrime`, `productOf`, `gcd`, `primesOf`, `primesOf3` or either `REGION_NAMES` dictionary (D-03, D-04).

**(a) Add a `factorize(n)` helper** next to `gcd` in the number-theory section at the top of the IIFE. It returns the ascending multiset of prime factors of `n` as an array: `1` and anything below `2` return `[]`; otherwise trial-divide by 2 and then by successive odd divisors, pushing each divisor once per time it divides, and push the surviving cofactor if it exceeds 1. Bound the divisor loop with a module-level `FACTOR_LIMIT` of `100000` (mitigates `T-g4b-02`): when a divisor reaches the limit the loop stops and the remaining cofactor is pushed unfactored, so the printed tail may be coarser than a full factorization but its product is still exactly the value printed before it — that identity must hold unconditionally and is asserted in verify. Style it to match the file: `var`, 2-space indent, no arrow functions, no comment containing a word this plan's gates negative-grep.

**(b) Add a small line builder** — one local helper used by both renderers, placed just above `renderProducts`. Given an ascending factor array and its product it returns the side-box tail: empty array returns `'1'`; a one-element array returns just that element as a string (no redundant `= product`); two or more elements return `factors.join(' * ') + ' = ' + product`. Build the ascending array with `.concat(...)` — which already returns a fresh array — followed by `.sort(function(a, b){ return a - b; })`. Never sort a `state.regions*` array in place.

**(c) Rewrite `renderProducts` (two-circle).** Keep the three `primesOf` reads and keep `leftTotal`/`rightTotal` computed exactly as they are today, so the gcd framing is untouched. Then:
  - `productLeftEl.textContent = 'A = ' + <side tail for sort(leftOnlyList.concat(sharedList)), leftTotal>`
  - `productRightEl.textContent = 'B = ' + <side tail for sort(rightOnlyList.concat(sharedList)), rightTotal>`
  - The middle box: start from `REGION_NAMES.overlap + ' = ' + leftTotal + ' ∩ ' + rightTotal + ' = ' + g` where `g = gcd(leftTotal, rightTotal)`; then compute `factorize(g)` and, **only if it has two or more entries**, append `' = ' + those.join(' * ')`. Reusing `REGION_NAMES.overlap` rather than a fresh `'A ∩ B'` literal is deliberate — it keeps one source of truth for the set names Task 1 established.
  - Per the ordering note in `<decisions>`: the tail comes from `factorize(g)`, never from `sharedList`. Do not substitute the lens contents here even though they usually coincide.

**(d) Rewrite `renderProducts3` (three-circle).** Keep the seven `primesOf3` reads and keep `totalA`/`totalB`/`totalC` computed exactly as they are today. Then:
  - `productAEl` gets `'A = ' + <side tail for sort(aOnly.concat(ab, ac, abc)), totalA>`; `productBEl` and `productCEl` the same shape with their own region groups and totals. The `with A` / `with B` / `with C` / `all three` notes and the `REGION_NAMES3.*` notes disappear entirely (D-02) — `REGION_NAMES3` itself stays, because the SVG captions, the `aria-label`s and the status messages still use it.
  - The four intersection boxes follow the two-circle intersection shape, with the label written **spaced**: `'A ∩ B = '`, `'A ∩ C = '`, `'B ∩ C = '`, `'A ∩ B ∩ C = '`. This is a deliberate change from today's tight `'A∩B'`, so that every box on the page — both modes — reads with the same spacing as the two-circle box that inherits it from `REGION_NAMES.overlap`. The value chain is unchanged: `totalA + ' ∩ ' + totalB + ' = ' + gcd(totalA, totalB)`, and for the triple `totalA + ' ∩ ' + totalB + ' ∩ ' + totalC + ' = ' + gcd(gcd(totalA, totalB), totalC)`. Each gets the same conditional `factorize` tail. Keep the existing `if (productXEl)` null guards.
  - Assign every box with `textContent`, never `innerHTML` (mitigates `T-g4b-01`).

**(e) Delete `formatSegments`** (lines 436-448) once both renderers no longer call it — the same treatment quick task 260926-eod gave `formatSide`. Deleting it before (c) and (d) are done will blank every box on load, so do it last.

One deviation from the user's brief to note in the summary, not to correct: their example wrote the first box as `A = 2 * 5 * 2 = 20` while this plan prints `A = 2 * 2 * 5 = 20`. Same multiset, same product, ascending order — deterministic output is worth more than preserving the incidental order in which they typed the factors.
  </action>
  <verify>
    <automated>Red-then-green: the harness run against the unmodified file exits non-zero on the first expected string, and the same harness run after the edits exits 0 for **Seed U** — the three two-circle boxes equal `A = 2 * 2 * 5 = 20`, `B = 2 * 5 * 7 = 70`, `A ∩ B = 20 ∩ 70 = 10 = 2 * 5` under strict string equality. This is the user's own worked example, reproduced verbatim (D-02, D-05).</automated>
    <automated>Seed D over the real repo file: the harness exits 0 with all ten `span.product-value` strings matching the Seed D list in `<behavior>` exactly, across `?mode=two` and `?mode=three`.</automated>
    <automated>Arithmetic-truth gate — parse, do not eyeball. With `node`, for each of the ten box strings collected from the Seed D dumps: split on ` = `; assert every segment that is a `*`-joined product of integers multiplies to the integer in the segment that follows it; assert every segment containing ` ∩ ` has all-integer operands whose gcd equals the integer that follows; and assert no box contains `(`, `)`, the substring `empty`, or two consecutive ` = `. Assert the loop actually examined 10 boxes. Print `PASS 10`; exit non-zero on any failure.</automated>
    <automated>Dead-helper gate, from the repo root: `grep -c formatSegments "Venn Diagrams/venn-diagrams.html"` returns 0.</automated>
    <automated>New-helper presence gates, from the repo root: `grep -c 'function factorize' "Venn Diagrams/venn-diagrams.html"` returns 1; the divisor-ceiling constant named in step (a) appears at least twice (its declaration plus at least one use in the loop condition); `grep -c 'function gcd' "Venn Diagrams/venn-diagrams.html"` returns 1 and `grep -c 'function productOf' "Venn Diagrams/venn-diagrams.html"` returns 1, proving both pre-existing helpers were left untouched.</automated>
    <automated>Diff-scope gate. Run `git` on its own line so its exit status cannot be swallowed by a pipeline: `git diff -- "Venn Diagrams/venn-diagrams.html" > "$SP/diff.txt"; test $? -eq 0 || exit 1`. Then assert `grep -cE '^[+-].*(innerHTML|viewBox|CENTERS3|CHIP_ANCHOR3|CAPTION_ANCHOR3|REGION_ANCHOR|state\.regions|localStorage|STORAGE_KEY)' "$SP/diff.txt"` finds no such changed line — proving this task touched no markup, geometry, storage or `innerHTML`.</automated>
    <automated>Bounded-factorization gate: with `node`, transcribe the newly added `factorize` implementation into a scratch file and assert `factorize(1)` is `[]`, `factorize(2)` is `[2]`, `factorize(20)` is `[2,2,5]`, `factorize(2618)` is `[2,7,11,17]`, `factorize(9999999967)` returns in under 500ms, and that for 200 random integers in `[1, 10**12]` the product of `factorize(n)` equals `n` exactly (the identity `T-g4b-02` relies on).</automated>
    <automated>Palette gate (baseline is zero, so any hit is new): `grep -nEi 'rgba?\(|hsla?\(|#[0-9a-f]{3}([0-9a-f]{3})?\b' "Venn Diagrams/venn-diagrams.html" | grep -vE '/\*|<!--' | wc -l` returns 0. Dependency gate: `grep -c 'script.*src' "Venn Diagrams/venn-diagrams.html"` returns exactly 1 and the matching line references `../assets/theme.js`. Task 1 gate still holds: `grep -cE '\b(Left|Right)\b' …` still returns 0.</automated>
  </verify>
  <done>Both renderers emit one short arithmetic line per box; Seed U reproduces the user's three requested lines verbatim; Seed D matches all ten expected strings in both modes; every printed factorization multiplies to the value it follows and every printed intersection equals the gcd of its operands; `formatSegments` is gone; `factorize` is bounded and product-preserving; no colour, dependency, geometry, storage or `innerHTML` change appears in the diff.</done>
</task>

<task type="auto">
  <name>Task 3: Probe the degenerate cases and confirm in a real browser</name>
  <files>Venn Diagrams/venn-diagrams.html</files>
  <precondition>Tasks 1 and 2 are committed; this task judges their combined output and only edits the file if a probe fails.</precondition>
  <reversibility rating="reversible">Verification sweep; any fix is a further string or line-builder tweak in the same file.</reversibility>
  <action>
This task is where D-05 is discharged: rendering is the only proof this repo can offer, so the sweep below ends in a real browser and a human's eyes.

Reuse Task 2's harness — the seed-substitution mechanism is already built — and run it over four more two-circle seeds that Seed U and Seed D do not reach. Expected output for each, to be asserted by strict string equality:

- **Seed E, everything empty** (A-only `[]`, lens `[]`, B-only `[]`): `A = 1`, `B = 1`, `A ∩ B = 1 ∩ 1 = 1`. No stray `*`, no dangling ` = `, no `(empty)`.
- **Seed S, one prime per side, empty lens** (A-only `[3]`, lens `[]`, B-only `[7]`): `A = 3`, `B = 7`, `A ∩ B = 3 ∩ 7 = 1`. Proves both the no-redundant-tail rule for a single factor and the no-tail rule for a coprime intersection.
- **Seed Q, the gcd-framing quirk** (A-only `[3]`, lens `[]`, B-only `[3]`): `A = 3`, `B = 3`, `A ∩ B = 3 ∩ 3 = 3`. The lens renders empty while the box prints 3 — the pre-existing property described in `<decisions>`. Assert the string, do **not** change the tool to make the lens agree, and record the observation verbatim in the summary as something the user may want to decide about in a later task.
- **Seed L, a full lens** (A-only `[]`, lens `[2, 2, 3]`, B-only `[]`): `A = 2 * 2 * 3 = 12`, `B = 2 * 2 * 3 = 12`, `A ∩ B = 12 ∩ 12 = 12 = 2 * 2 * 3`. Proves the tail appears when the gcd is composite and that a repeated prime prints twice on both sides.

Then run the interaction and regression sweep against the real repo file, and finally hand it to a human.

If any probe fails, fix it in `Venn Diagrams/venn-diagrams.html` within the rules already set (side-box and intersection-box formats per D-02, no change to `gcd`, the region model, storage keys or geometry) and re-run every gate in Tasks 1, 2 and 3 before declaring done. Delete the scratchpad copies at the end; confirm `git status --porcelain` shows only `Venn Diagrams/venn-diagrams.html` plus the expected `.planning/` artifacts.
  </action>
  <verify>
    <automated>Four-seed probe: the harness exits 0 for Seeds E, S, Q and L with the three two-circle box strings matching the expectations above exactly. Assert it compared 3 strings per seed (12 total) so a skipped seed fails loudly.</automated>
    <automated>Interaction sweep, driven by a `node` script appended to a scratchpad copy (registering its own `load` listener so it runs after the tool's): with the default seed in two-circle mode, synthesise a click on a `.prime-chip` then on `#region-overlap`, and assert the three box strings changed and still parse under Task 2's arithmetic-truth checks; then click `#clear-btn` and assert the boxes read `A = 1`, `B = 1`, `A ∩ B = 1 ∩ 1 = 1`; then click `#mode-three` and assert `#products-three` is no longer `hidden` and its seven boxes still parse. Exit non-zero on any failure.</automated>
    <automated>Persistence and console sweep against the real file: load `?mode=three` with a **persistent** `--user-data-dir`, then load again with the same profile and no `mode` param, and assert the three-circle panel is the visible one (per-mode persistence survived). Separately, load both modes with `--enable-logging=stderr --log-level=0` and assert no `ERROR:CONSOLE` line appears.</automated>
    <automated>Four headless screenshots at `--window-size=900,900` with a fresh `--user-data-dir` — `?theme=day&mode=two`, `?theme=night&mode=two`, `?theme=day&mode=three`, `?theme=night&mode=three` — all four produced and non-empty, and all four retained in the scratchpad so the human check below has something to compare against.</automated>
    <human-check>Open `Venn Diagrams/venn-diagrams.html` in a real browser from a `file://` URL. In two-circle mode confirm: the two circle labels read A and B; the three region captions read `A \ B`, `A ∩ B`, `B \ A`; the lede introduces the circles as A and B; the three boxes are one short arithmetic line each with no parentheses and no prose. Place a prime by clicking, and again by dragging, and confirm the boxes update and the status message names the region in the new notation. Press Clear all and confirm the boxes read `A = 1` / `B = 1` / `A ∩ B = 1 ∩ 1 = 1`. Switch to three circles and confirm the seven boxes read the same way with spaced `∩`. Toggle day/night in both modes and confirm nothing lost its colour. Confirm no word naming a circle by its screen position appears anywhere on either view.</human-check>
  </verify>
  <done>All four degenerate seeds print true, tail-free-where-appropriate arithmetic; clicking, dragging, clearing, mode switching and per-mode persistence all still work with no console error; both themes render in both modes; a human has confirmed the rendered page by eye; the gcd-framing observation from Seed Q is written into the summary; and the working tree contains no leftover scratch file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → page state | Persisted JSON under `venn-diagrams`, `venn-diagrams-three`, `venn-diagrams-mode` is re-read on every load and crosses into `state.regions*`; it is attacker-writable by anything running on the same local origin |
| prime picker / drag-drop → state | User-chosen integers cross into region arrays; `isPrime` and the `MAX_PER_REGION` caps are the filter |
| computed value → rendered box text | The new `factorize()` output and the gcd values cross into DOM text nodes under the diagram |
| `file://` document → outside world | None. No network call, no server, no third-party script beyond Google Fonts and the local `../assets/theme.js` (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g4b-01 | Tampering | new box strings in `renderProducts` / `renderProducts3` | medium | mitigate | Every box string is assigned with `textContent`; `innerHTML` is forbidden for the new strings and the Task 2 diff gate greps for it, so a tampered `localStorage` payload cannot inject markup through a box |
| T-g4b-02 | Denial of Service | new `factorize()` trial division | low | mitigate | The divisor loop is bounded by `FACTOR_LIMIT = 100000` and pushes the surviving cofactor unfactored, so a tampered store carrying a large prime cannot hang the tab; the product identity of the printed tail is preserved unconditionally and is asserted over 200 random integers in Task 2 |
| T-g4b-03 | Tampering | `localStorage` keys `venn-diagrams`, `venn-diagrams-three`, `venn-diagrams-mode` | low | accept | Same-origin, self-owned, unprivileged store; the existing `isValidStoredRegion`/`isValidStoredRegion3` primality-and-count validation is retained byte-identical (D-03) and the worst case is a wrong diagram in the tamperer's own browser |
| T-g4b-04 | Information Disclosure | headless-Chrome probe copies and screenshots | low | mitigate | Every harness artifact is written to `mktemp -d` scratch, never into the repo, and Task 3 asserts `git status --porcelain` shows no leftover file before the task is done |
| T-g4b-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install occurs anywhere in this plan — the task edits one existing HTML file and adds no dependency, so the supply-chain surface is unchanged (D-04) |
</threat_model>

<verification>
- `grep -cE '\b(Left|Right)\b' "Venn Diagrams/venn-diagrams.html"` returns 0 (was 6).
- `grep -c formatSegments "Venn Diagrams/venn-diagrams.html"` returns 0.
- Seed U reproduces the user's three requested lines verbatim: `A = 2 * 2 * 5 = 20`, `B = 2 * 5 * 7 = 70`, `A ∩ B = 20 ∩ 70 = 10 = 2 * 5`.
- Seed D matches all ten expected box strings across both modes.
- Seeds E, S, Q and L match their twelve expected strings.
- Every printed factorization multiplies to the value it follows; every printed intersection equals the gcd of its operands.
- Positional identifiers, storage keys, geometry constants, `viewBox` values and the `gcd`/`productOf`/`isPrime` helpers are unchanged.
- No literal colour, no new dependency, no new file; `file://` load is console-clean in both modes and both themes.
- Human browser pass completed.
</verification>

<success_criteria>
The Prime Venn Diagram tool calls its two-circle sets A and B in every place a reader can see — labels, captions, `aria-label`s, status messages, lede and boxes — while every positional identifier in the code is untouched; and every info box in both modes is a single short arithmetic line, reproducing the user's worked example character for character, with every printed product and intersection numerically true including in the empty, single-prime, coprime and repeated-prime cases.
</success_criteria>

<output>
Create `.planning/quick/260926-g4b-venn-diagrams-rename-left-right-set-labe/260926-g4b-SUMMARY.md` when done.

Record in it: the Seed U output verbatim; the before/after of all ten default-seed boxes; the Seed Q observation about the gcd framing printing a non-1 intersection over an empty lens, flagged as a pre-existing property the user may want to revisit; the confirmation that the tool has only two-circle and three-circle modes and no plain-set-elements mode (so the user's `A = {1, 2, 3}` alternative format was not needed); and the note that factor order is printed ascending rather than in the order the user typed it in their example.
</output>
